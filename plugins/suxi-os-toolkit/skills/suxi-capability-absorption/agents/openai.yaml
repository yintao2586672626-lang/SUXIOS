interface:
  display_name: "宿析能力吸收与复刻"
  short_description: "先区分储存与吸纳，再把过门能力做成可验证功能"
  default_prompt: "使用 $suxi-capability-absorption 按资料单元判断应储存、候选吸纳、正式吸纳还是事实入库，并只把通过三道门的能力融入宿析OS。"
policy:
  allow_implicit_invocation: true
