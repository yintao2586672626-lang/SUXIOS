interface:
  display_name: "宿析携程字段闭环"
  short_description: "打通单个携程接口从响应证据到字段、入库、UI状态和验证器的闭环"
  default_prompt: "使用 $suxi-ctrip-field-table-closure 将携程接口证据转换为宿析OS指标字段、表、UI状态和验证。"
policy:
  allow_implicit_invocation: true
