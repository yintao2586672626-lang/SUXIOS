interface:
  display_name: "宿析 OTA 运营"
  short_description: "携程/美团数据、浏览器采集、Cookie、渠道运营处理规则"
  default_prompt: "Use $suxi-ota-ops to handle OTA data, Ctrip/Meituan browser capture, channel, cookie, traffic, order, review, or field-mapping work in 宿析OS."
policy:
  allow_implicit_invocation: true
