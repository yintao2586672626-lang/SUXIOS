# TypeScript 类型声明目录
# ========================
# 所有全局类型定义、接口声明、枚举等统一放在此目录
# 
# 使用方式：
#   import type { User, ApiResponse } from '@/types'
#
# 文件说明：
#   - global.d.ts      全局类型扩展（Vue、DOM 等）
#   - api.ts            API 请求/响应类型
#   - hotel.ts          酒店相关业务类型
#   - user.ts           用户/权限相关类型
#   - online-data.ts    线上数据获取相关类型
#   - common.ts         通用工具类型（分页、状态码等）
