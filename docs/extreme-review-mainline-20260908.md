# 深度审查修复的主线整合

本次候选从 `origin/main` 的 `7cec7c2cb22694457652e3b3e0f9771eed15704e` 建立，移植 2026-09-08 深度审查中适用于主线的修复。共享开发目录和冻结的 `HOTEL/` 均未改动；其他未提交功能以及 PR #43、#44 不属于本次提交。

## 用户可见问题与修复

| 原审查组 | 主线处理 | 结果及边界 |
|---|---|---|
| R01 携程请求与重试隔离 | 已移植并进一步修复 | 排名和流量请求同时校验会话、酒店上下文、请求序号和查询日期。旧请求不再覆盖新结果、错误提示或加载状态，重试等待期间切换范围会终止重试。同酒店替换请求也由统一请求序号隔离。 |
| R02 云浏览器租约恢复 | 已移植 | 收到完全匹配的租约身份后立即登记清理责任；浏览器启动、只读证明等后续校验失败仍关闭本次租约。其他租户、酒店、用户、日期、平台、会话归属或格式错误的租约不会被关闭。 |
| R03 延迟资源版本传播 | 已移植 | 正式构建从真实文件内容生成 3 个启动 loader 的 4 个组件版本及收益 AI helper 版本。编译期间文件变化会阻止发布，校验器能检出过期版本。未带入其他任务尚未合入的运营 helper 加载改造。 |
| R04 迁移锁缺项 | 已移植必要依赖 | 连同已有的重复索引清理迁移一并登记；保留同列同序的原始索引，仅删除重复的非唯一索引，不修改业务行。未对本地或生产业务库执行迁移。 |
| R05 产品指南链接 CSP 样式 | 主线不适用 | 主线尚无该未提交产品指南入口，因此没有引入无关入口和样式；原修复仍在开发目录和原审查增量中。 |
| R06 旧测试合同 | 按主线适配 | 更新本轮直接影响的加载状态和通知路由测试，补齐恢复测试 fixture 的读取权限。其他只针对未合入功能的测试调整不带入主线。 |
| R07 规则准确性 | 已移植 | 修正实际会话存储描述、凭证诊断边界和 Git 动作的授权说明。 |
| R08 局部结构改进 | 已移植 | 9 个通知路由移动到既有 WeCom 路由域，保留方法、路径、处理器和 Auth；采集缺失字段计数、美团请求归一化复用已有函数；热点限制收紧或新增登记，既有限制没有调高。 |

## 二次复核

- 原排名历史功能尚未进入主线，隔离计数器直接绑定主线已有的手动请求入口，不复制历史查询功能。
- 排名和流量共用请求归属计数，覆盖取消后同酒店启动新请求、旧 HTTP 请求随后返回的情况。
- 租约拒绝路径补充了全部身份维度、外部浏览器要求和用户浏览器关闭状态的回归。
- 所有运行产物由 `npm run build:frontend` 生成，未手工改压缩文件或版本哈希。
- 既有 SQL 已核对：两个索引都覆盖 `system_hotel_id, data_date, readback_verified, source, data_type`，无唯一约束变化。
- 提交 hook 首次拒绝了当前根规则中已被替换的旧文案。校验器现兼容已知的新旧规则版本，仍要求完整结果与诊断策略条款；没有跳过 hook 或删去鉴权、凭证、提交授权等校验。

## 本地验证

目标：同仓库隔离目录 `.review-worktrees/extreme-20260908-0d52`。使用 fixture/stub，不执行真实 OTA、PMS、LLM、消息或经营动作。

| 检查 | 结果 |
|---|---|
| 云租约、采集服务、通知与路由、异步恢复 PHP 回归 | PASS：108 测试、980 断言 |
| 手动请求隔离、版本传播、最小凭证 UI 合同 | PASS：91 测试 |
| 携程、美团、构建/版本、通知路由直接依赖 | PASS：399 测试；与前一组有交集，不相加冒充独立覆盖 |
| 正式前端构建 | PASS |
| canonical integration | PASS，含 2,291 项静态集成合同；该数量不是浏览器 E2E |
| 主线新鲜度 | PASS，候选创建及提交前主线独有提交为 0 |

日志保存在本次隔离目录的 `output/review-release-20260908/`。提交前还执行公开入口和精确暂存树检查；GitHub 完整套件、数据库迁移与浏览器检查以 PR 对应提交的 CI 结果为准。原开发快照的全量通过结果不能作为本候选的全量证据。

## 未改变的证据边界

本次目标是审核、GitHub 持久化和主线合并。生产部署、真实账号采集保存读回、员工权限现场、云浏览器现场及经营效果均未由本次验证证明。大型单文件等既有结构债务继续保留；本次只处理已复核问题和必要依赖。
