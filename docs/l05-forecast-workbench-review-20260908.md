# L05 时点回测与定价情景：独立审查记录

新增入口位于酒店数据分析师与运营工具箱的收益页面。用户可导入明确标记为人工待核验或 synthetic 的历史版本，执行 7/14/30 天时点回测、比较周同期和最近七日均值基线，并保存和精确回放价格假设方案。

## 提交范围

- 基于已在 GitHub 的 `codex/save-all-20260903` 提交 `9fa781fc92baa66b410f4e5d6e8c8c3c1ebae81b`，因此本 PR 依赖 #43。
- 从 L05 的固定输入快照中逐文件核对 SHA-256，分离其 25 个改动路径；未带入预置的 177 项其他改动。
- 组件导出和延迟加载按本批范围适配。模板、桥接文件和启动资源通过仓库构建脚本重新生成。
- 原正式预测审批和调价路径保持原有规则；新增工具不执行调价。

## 独立复核及修正

原前端只检查返回方案的酒店、渠道、门店和房型，未检查历史回读是否返回所请求的方案编号，也接受非布尔的回读标记或无效编号。两个新增行为测试在原实现上失败。

现要求保存响应包含有效内容编号和严格的 `readback_verified=true`；历史回读必须返回请求的同一编号。同范围另一份方案、无效编号和字符串校验标记均显示失败，不更新结果或已保存状态。有效方案仍可正常回放。

另补齐集成后启动组件包的生成，修复公共入口检查发现的过期产物。没有放宽测试或构建标准。

首次提交被规则资产钩子阻止：旧版钩子要求外层 AGENTS 中已经替换的两句旧文案。兼容当前完整交付规则及既有旧版规则，保留所有其他必需检查；不修改外层规则或跳过钩子。

新增五条路由触发了主路由文件的零增长约束。沿现有 domain 分片方式提取 Revenue AI 路由体，保留原注册顺序、URL、处理方法及外层 Auth 组；未提高文件限额。

完整 Node 回归进一步要求提取后立即收紧主路由零增长上限；已从 784 行降为实际 778 行，保留原测试的精确相等要求。

分片注册表测试因新增文件而失败，分类为本次授权功能所需的结构基线扩展：不是路由行为违反合同，也不是可在不改变期望清单下修复的测试代码错误。仅在既有顺序中增加新分片，原有条目、旧路由指纹和鉴权断言保持不变。

PHP 和 Node 两份分片登记清单均添加 Revenue AI 分片。旧有 129 条已提取接口的数量、SHA-256、顺序及 Auth 断言保持原值。

首轮 GitHub CI 发现收益前端合同测试只读取主路由文件，无法看到已注册的分片。分类为测试读取缺陷：实际参数化路由顺序正确，产品不违反顺序合同，也没有变更该合同的要求。改用仓库既有分片展开读取器，原先“参数化审批路由在通用保存路由之前”的断言原样保留；先本地复现再修正。

后端 RevenueAiRouteExposureContractTest 存在同类读取缺陷，也改用既有 PHP 分片读取器；旧审批动作不可公开、新审批接口必须存在的断言原样保留。新增路由消费回归覆盖 ControllerRouteContractTest 与该测试，共 18 项通过。

本批集成创建的路由分片还漏写了 strict_types 声明，首轮 CI 的 PHP 源码合同检查正确拦截；已补齐声明并复验，未增加例外清单。

## 已执行验证

GitHub 自动复查另发现两项 P1 和一项 P2，独立复现后修正：

- 原保存直接序列化整份导入内容，未拒绝额外字段。现在按证据、逐日版本和价格情景的字段合同校验，并拒绝叶子字段嵌套内容。未声明内容在创建存储目录前即失败，错误不回显输入值；旧文件含未声明输入时保留文件但停止回传。两项反例先失败，修复后通过。旧文件测试曾遗漏 JSON 浮点保留选项，属于测试构造缺陷；补齐同生产序列化选项后确认原实现确实失败，未改变预期行为。
- 原历史回读假定原始证据始终可见。正式权限策略会删除 `evidence`，现保留该策略，页面明确显示“仅显示摘要”，清空未恢复的输入，导入证据前禁用重新计算及保存。已用真实 ProtectedCapabilityService 的默认策略在隔离浏览器复现并验证摘要回读、重新导入恢复、保存及提示；这不等于完整 Auth 链或真实受限账号验收。非摘要响应若缺少原始输入也会被拒绝。
- 同一预测时点的不同价格方案原本使用相同历史按钮文案。现同时显示方案编号前缀、保存时间和预测时点，浏览器保存两份价格方案并确认能够准确选择原方案。

复查后的直接回归：15 项 PHP / 1399 断言、9 项前端状态测试通过；桌面和手机宽度的隔离浏览器流程通过。此前全量 315 个 Node 测试文件已完成一次串行回归，修改后的同提交 CI 另行复验。

第二轮还补齐三个实际缺口：方案默认位于版本目录、页面正式门禁未登记该组件、历史请求未区分未读/加载中/确认为空。现在生产模式或 `releases/<版本>` 目录运行时，必须设置发布目录之外的 `SUXIOS_FORECAST_PLAN_PATH`；缺失或无效配置会明确阻止保存/历史读取，预览仍可运行。配置值只作为服务器目录使用，不进入方案或响应。对路径进行绝对路径、发布目录和已有祖先符号链接检查。本地开发保留原 runtime 兼容路径；不自动迁移或删除旧文件。

部署时为应用用户准备专用外部目录，例如 `/var/lib/suxios/forecast-plans`，将 `SUXIOS_FORECAST_PLAN_PATH` 设置为该绝对路径，并将其纳入备份；升级和回滚使用同一路径。该配置不放在可公开访问目录、发布包、`current` 或 `releases` 下。有旧版方案时应在维护窗口保留来源及内容身份进行迁移核对，禁止直接覆盖同编号不同内容。此次未执行部署或真实文件迁移。

配置路径回归实际切换两个隔离 runtime 路径，验证切换与回滚后同一历史及新方案均可精确读回；配置缺失、相对路径和应用内路径失败关闭，预览仍可用。历史加载状态与空状态均通过前端状态测试及延迟请求浏览器验证。正式 business-page-contract 的 agent-center 已登记本组件源码锚点和聚焦行为回归，旧有登记与断言保留。

第二轮最终直接验证：22项PHP / 1427断言通过；正式页面门禁实际执行239项测试全部通过，其中包含新增工作台行为回归。源码版本更新后先重建组件桥与启动辅助包，再复验实际浏览器；未手改生成哈希或放宽缓存身份断言。

第二轮CI的整页业务链又暴露既有启动时序缺口：配置列表接口早于延迟模块返回时，会调用尚未注册的 `buildCtripConfigFormForHotel`，并在异常前就标记列表已加载。保留原CI失败记录及同提交重跑；另用真实启动桥和配置加载函数，控制模块延迟构造两个确定性反例，原实现均失败。现在在回填配置前等待既有延迟资源加载器，等待后复核请求/会话是否仍有效；模块缺失明确失败，过期响应不回填。配置回填、既有最小凭证界面合同与会话隔离共97项回归通过，未读取真实凭证、改变认证或放宽页面错误断言。最终生成入口及整页CI继续按新提交验证。

| 范围 | 命令或方法 | 结果 |
| --- | --- | --- |
| PHP 功能及直接依赖 | PHPUnit：Workbench 两项、TemporalInsight 两项、Trial、Readiness、Pricing、PricingSpreadsheet 测试文件 | 76 tests / 2321 assertions，通过 |
| 路由与依赖隔离 | RouteDomainManifest、ComposerWorktreeAutoload、PhpunitBootstrapIsolation | 6 tests / 43 assertions，通过 |
| 前端状态和定价直接依赖 | `node --test tests/automation/revenue_forecast_workbench.test.mjs tests/automation/revenue_ai_static.test.mjs tests/automation/room_type_pricing_guard_ui.test.mjs` | 64 tests，通过；含两个先失败后修复的反例 |
| 浏览器与控制器 | `node tests/automation/revenue_forecast_workbench_browser.mjs` | 真实组件桥、Vue、PHP 控制器、隔离 SQLite；1440px 和 390px 通过 |
| 页面操作 | 运行、保存、修改输入、读取原版本、错误恢复、跨酒店拒绝、匿名拒绝 | 通过；无页面错误，无整页横向溢出 |
| 生成资源 | 仓库模板、组件桥、入口与启动辅助包构建；模板、入口、启动包校验 | 通过 |
| 集成与入口 | 2291 项静态集成合同、公共入口 guard、前端性能预算 | 通过；静态合同不代替浏览器验收 |

## 证据与限制

以上属于本地独立代码、自动测试及 synthetic 页面/API 证据。导入历史的可见时刻及来源尚未经真实采集链核验；当前入口明确拒绝将手工输入标为 verified。

预测对象是指定渠道、门店和房型的净入住间夜，不是全酒店入住率或不受库存约束的潜在需求。价格弹性、全周期库存和价格是假设，金额不代表因果增收。历史方案保留输入和结果，不在回读时重新计算。

真实来源适配、完整登录应用的跨任务验收、生产持久目录/备份配置、部署、真实预测表现及经营效果尚未验证。方案按租户、酒店、渠道、门店和房型隔离；生产只接受外部持久目录，本地开发 runtime 兼容路径不代表生产配置已完成。

GitHub 推送、PR 和同提交 CI 状态另以 GitHub 实际记录为准。
