# 宿析OS 深度审查与重大问题修复跟进

- 审查日期：2026-07-30
- 应用根目录：`HOTEL/`
- 冻结分支：`agent/fix-daily-review-gates`
- 审查起始冻结 HEAD：`bf8dc9434f0de27ec45713d496ef0f94821f51e5`
- 最终验证基线 HEAD：`fb622d04da43e72fe213181c1d23ceb3975dde2f`
- 处理边界：保留所有用户与并行任务的未提交变更；本审查任务未执行 reset、checkout、clean、提交、推送、PR、部署、真实 OTA 采集或生产发送
- 审查目标：优先修复会阻断核心功能、泄漏高权限能力、把缺失数据伪装成完整事实，或让用户触发无效操作的已证实问题

## 1. 结论

本轮没有发现新的、仍未处理的已证实 P0。已证实并完成最小修复的主要问题有五项：

1. 普通账号进入页面时仍会自动请求 Revenue AI 高权限接口，形成不必要的 401/403、错误噪音与状态污染。
2. 携程、美团组合指标在一侧缺失时会丢弃另一侧已知值，用户无法分辨“全缺失”和“已知部分合计”。
3. 当前酒店没有完成唯一 PMS 绑定时，“实时同步”按钮仍可操作，用户会触发一个必然失败的路径。
4. 合法 UI 迁移后，六个 Node 合同仍匹配旧结构，导致完整回归出现假失败。
5. `operation-static.js` 内容已更新，但主入口仍声明旧缓存版本，浏览器可能复用与当前主脚本不匹配的旧运行时代码。

同时，当前共享工作区中其他功能任务修复了通知页重新挂载、发送记录重试语义、全局当前酒店联动等问题。本报告只把这些作为当前快照事实，不把其他任务的贡献归入本轮直接修改。

在规则与架构层面，没有证据支持当前进行全量重构。最主要的风险仍是：

- 主前端脚本和模板体积大、多人共享写入时构建容易漂移。
- 本地环境使用 `development_fallback`，缓存与锁为 `not_enforced`；本地可用不等于生产多实例安全。
- 真实 OTA 目标日、同店同日三源、生产定时器、真实企业微信回执、部署与 CI 仍必须与代码/测试证明分层。

## 2. 快照与并发保护

审查开始时工作区已经包含大量已跟踪和未跟踪改动，覆盖 OTA、PMS、通知、知识库、规则、脚本、前端和测试。处理原则如下：

- 只在有直接静态证据或失败夹具的位置做增量修改。
- 不恢复原有删除文件，不清理未跟踪文件，不读取原始登录会话或 Cookie/token 报告。
- 写入前检查目标文件状态；发现共享前端仍变化时暂停构建。
- 构建器因源文件在构建期间漂移而拒绝生成时，保留最新源码，不绕过保护、不放宽门禁。
- 报告中的测试数字只代表明确执行过的快照，不把并行任务的口头结果当作本轮独立验证。

## 3. 发现与修复

### F-01 — P1 — 普通账号自动请求 Revenue AI

#### 已证实根因

页面初始化会调用 Revenue AI overview；调用前没有与后端能力一致的前端权限门禁。普通账号因此也会发起高权限请求。

#### 影响

- 产生可避免的 401/403 与错误日志。
- 页面初始化状态可能被无权限接口失败污染。
- 高权限能力的展示与实际授权边界不一致，增加后续误用风险。

#### 最小修复

- 增加统一 `canUseRevenueAi()`，只接受超级管理员或显式 `can_use_ai_decision`、`ai.view`、`ai.execute` 权限。
- `loadRevenueAiOverview()` 在发起网络请求前直接返回，不用“请求失败后隐藏”代替权限控制。
- 增加负向回归，验证普通账号不会触发网络调用。

#### 状态

已修复；目标 Node 回归通过。

### F-02 — P1 — 双 OTA 部分事实被误表达

#### 已证实根因

组合指标只有在携程与美团两侧都返回时才计算合计；任一平台缺失时，已知平台的值也被丢弃，并统一表现为未返回。

#### 影响

- 把“携程已知、美团缺失”错误压扁成“完全未知”。
- 用户无法使用已验证的部分事实，也看不到缺口属于哪个平台。
- 下游文案可能把部分数据误当完整合计，或反过来隐藏已知事实。

#### 最小修复

- 对已知平台值求部分小计。
- 缺少任一平台时设置 `dataStatus: partial`。
- 明确显示 `合计不完整` 与缺失平台，不以 `0` 填充缺口。
- 两侧都缺失时保持 missing；两侧齐全时才标记完整。

#### 状态

已修复；新增运行时函数夹具覆盖完整、部分、全缺失三种情况。

### F-03 — P1 — 未绑定 PMS 时仍可点击实时同步

#### 已证实根因

实时同步按钮只受加载状态约束，没有要求当前酒店 `binding_status === 'configured'`。

#### 影响

- 用户进入一个注定失败的操作路径。
- 失败看起来像系统或网络故障，实际是配置前置条件未满足。
- 多酒店场景中容易把“当前酒店未绑定”误判成 PMS 服务不可用。

#### 最小修复

- 唯一 PMS 绑定未配置时禁用实时同步。
- 按钮 title 明确提示“请先为当前门店配置唯一 PMS”。
- 保留酒店级绑定状态，不使用其他酒店的配置兜底。

#### 状态

已修复；PMS 目标回归通过。

### F-04 — P2 — 合法 UI 迁移后测试守卫仍匹配旧结构

#### 已证实根因

六处 Node 合同测试仍依赖旧文案、旧 Tailwind 类、旧 DOM 结构或旧酒店入口：

- 运营自动化中心仍要求旧的“选择通知群”文案。
- 携程当前酒店 UI 仍按旧的单行 DOM 匹配。
- PMS 使用记录只允许旧独立页面。
- AI 工作台未考虑三源驾驶舱模式的隐藏门禁。
- 美团 PMS 测试硬编码旧按钮颜色。
- 美团空状态仍要求旧文案。

#### 影响

- 真实功能正确但完整 Node 套件产生假失败。
- 团队可能为“让测试变绿”回退已经完成的真实 UI 迁移。
- 发布守卫失去区分功能回归与合同陈旧的能力。

#### 最小修复

只更新合同到当前真实语义，不降低安全、酒店隔离、数据真实性或操作门禁：

- 断言每酒店唯一 Webhook 推送通道。
- 容忍当前 span 布局但继续验证当前酒店约束。
- 允许完整 PMS 页面或驾驶舱 PMS 模式记录使用。
- 明确三源驾驶舱下隐藏独立 AI 数据卡。
- 使用语义类名校验按钮。
- 校验当前真实空状态文案。

#### 状态

已修复；六个原失败测试的目标重跑均通过。

### F-05 — P1 — 运行时脚本缓存版本落后于真实文件

#### 已证实根因

`public/operation-static.js` 当前 SHA-256 前缀为 `7eb502590a`，但 `public/app-main.js` 仍声明旧版本 `h41c2131a16`。内容变更与缓存键没有同步，完整 Node 回归据此失败。

#### 影响

- 已缓存客户端可能继续执行旧运营运行时脚本。
- 新主脚本与旧运行时帮助函数混用时，运营页可能表现为入口已更新、具体交互仍停留在旧逻辑。
- 测试、源码和浏览器实际加载产物不再指向同一版本。

#### 最小修复

- 将 `operationStaticScriptVersion` 更新为真实文件哈希前缀 `h7eb502590a`。
- 重新生成 `public/app-main.min.js` 与 `public/index.html` 的受控入口产物。
- 不修改运行时业务逻辑，只恢复源码、压缩产物、缓存键和入口引用的一致性。

#### 状态

已修复；当前声明前缀与文件 SHA-256 前缀一致，携程公开资料目标回归 8/8、`verify:public-entry` 和完整 Node 均通过。

## 4. 稳定性、规则与架构结论

### 4.1 启动与健康

本轮复核时：

- `GET /api/health` 返回 HTTP 200。
- application、database、database schema、competitor report idempotency 为 `ok`。
- local state 为 `development_fallback`。
- cache 与 lock 为 `not_enforced`。

结论：本地开发栈可用；这不是生产缓存、分布式锁、单实例、定时器或持久状态的证明。

### 4.2 资源与构建

共享前端在并行任务持续写入时多次触发源/产物漂移保护。该保护避免把不同时刻的源码、模板与压缩产物混为一版，属于正确失败。

已观察到的风险放大器：

- `public/app-main.js` 与合并模板仍是高耦合热点。
- 构建需要统一所有 fragment、渲染产物、入口 hash 和 Tailwind 产物。
- 最终模板守卫输出：模板 2,173,136 bytes，渲染产物 1,424,528 bytes，渲染 gzip 254,054 bytes。
- 启动 gzip 为 639,820 bytes，已进入警告区间；距离 650,000 bytes 硬限制仅余 10,180 bytes。

当前不进行大文件拆分重构：没有证据证明它是本轮功能故障根因，而且脏工作区中大规模重构会显著增加覆盖和回归风险。

### 4.3 数据真实性

本轮继续坚持：

- 缺失不转换成 `0`。
- 单平台已知值可以显示，但必须标记 `partial`，不能称为完整双平台合计。
- OTA 事实只支持渠道分析，不上升为全酒店收入、入住率或利润。
- PMS、订单、夜审房费、实收、应收、核销、会计收入是不同阶段，不能相互替代。
- AI 只接收授权范围内、带来源/酒店/日期/质量状态的输入；无权用户不发起 AI 请求。

### 4.4 规则与守卫

当前规则方向一致：

- `AGENTS.md` 与协作章程均要求功能优先、真实状态、酒店隔离和最小改动。
- `rules/` 负责权限与边界，`hooks/`/package scripts 负责可执行守卫。
- 守卫应随真实结构演进；更新陈旧合同，不删除规则历史、不放宽事实边界。
- “测试通过”只能证明当前代码合同，不替代外部账号、真实采集、保存回读、真实送达或部署证明。

### 4.5 架构链路

当前可接受的主链仍是：

```text
携程/美团渠道事实
  -> 酒店、平台、门店、业务日期、来源、质量状态
  -> 收益分析
  -> 授权后的 AI 判断
  -> 运营动作与通知
  -> 投资分析
```

关键边界：

- OTA 层不生成全酒店事实。
- PMS/订单事实缺失时，收益层不得反推完整收入。
- AI 层不绕过查看/执行权限。
- 通知层的“已保存”“HTTP 成功”“已送达”必须分开。
- 投资层只能消费定义清楚、范围一致的指标。

## 5. 验证记录

| 验证项 | 结果 | 证据边界 |
|---|---:|---|
| 新增功能完整性回归 | 3/3 通过 | Revenue AI 权限、双 OTA 部分合计、PMS 未绑定门禁 |
| 最终前端目标回归 | 135/135 通过 | 核心前端、OTA、PMS、通知合同 |
| 六个陈旧合同目标重跑 | 全部通过 | 只证明已更新的 Node 合同 |
| PHP 目标回归 | 197 tests / 1587 assertions，通过 | 受影响服务与高风险边界 |
| 完整 PHPUnit | 3040 tests / 27910 assertions / 0 failures / 1 skipped | 退出码 0；执行期间 `app/route/tests` 无源码写入 |
| 完整 Node | 185 files / 1118 tests / 0 failures | 退出码 0；修复缓存版本后重跑 |
| 前端模板、Tailwind、公开入口、上下文资产守卫 | 4/4 通过 | 四个 npm 守卫退出码均为 0 |
| `/api/health` | HTTP 200，overall `ok` | application、database、schema、幂等检查为 `ok`；本地状态不是生产证明 |
| `git diff --check` | 通过 | 两份迁移脚本的 EOF 空行已机械清理，未修改 SQL 语义 |

## 6. 未验证与剩余阻塞

以下事项不能由本轮代码与本地测试替代：

1. 当前目标酒店、目标业务日的携程与美团真实账号采集。
2. PMS/订单来了当前会话、同店同日保存及数据库回读。
3. 三源事实齐全后生成的真实运营判断。
4. 生产企业微信群真实发送与人工收件回执。
5. 生产定时器启用状态、漏跑恢复与多实例幂等。
6. 生产持久缓存、锁和强制持久状态。
7. 部署、远端分支、PR 与 CI。

这些项目当前应保持 `blocked` 或 `unverified`，不能写成失败、成功或零值。

## 7. 本轮直接修改范围

- `public/app-main.js`
- `public/app-main.min.js`
- `public/index.html`
- `resources/frontend/templates/fragments/15aab-page-pms-operating-data.html`
- `tests/automation/functional_integrity_guard.test.mjs`
- `tests/automation/auth_session_dashboard_isolation.test.mjs`
- `tests/automation/ui_truth_semantics.test.mjs`
- `tests/automation/operations_automation_center_ui.test.mjs`
- `tests/automation/access_tier_permissions.test.mjs`
- `tests/automation/pms_hotel_sorting.test.mjs`
- `tests/automation/dashboard_hotel_data_cockpit.test.mjs`
- `tests/automation/meituan_cloud_pms_integration_ui.test.mjs`
- `tests/automation/meituan_owner_workspace.test.mjs`

另外，机械清理了以下迁移脚本各一个 EOF 空行；清理后文件与当前 HEAD 一致，没有保留 SQL 语义改动：

- `database/migrations/20260729_seed_traffic_operation_management_golden_sentences.sql`
- `database/migrations/20260730_seed_hotel_revenue_success_practices_extension.sql`

## 8. 停止条件

以下闭环均已满足，本轮在此停止，不扩展新功能：

1. 最新共享前端源码在完整 Node 执行期间保持稳定。
2. 统一构建、目标 Node、前端守卫和完整 Node 均通过。
3. 最终健康检查为 HTTP 200。
4. 报告已补齐最终测试数字与剩余边界。

## 9. 当前业务链复核与增量修复（2026-07-30）

本节记录第 8 节之后共享工作区继续产生的业务链增量，作为当前收口结论。它不改变“代码和测试证据不能冒充真实采集、发送或生产启用”的边界。

### 9.1 当前快照与并发边界

- 分支：`agent/fix-daily-review-gates`
- HEAD：`af5690d5084dcab014cffd260f22500104dfb8dc`
- 收口检查时 `git status --short` 有 119 条记录；这是共享脏工作区，不代表本节新增了 119 个文件。
- 未执行 `reset`、`checkout`、`clean`，没有回退或覆盖其他任务的修改。
- 高风险目标服务和房型页面在测试前连续 8 秒哈希保持稳定；测试期间只在该稳定快照上读写构建产物。
- 本轮没有提交、推送、创建 PR 或部署。当前 HEAD 的远端同步来自另一项用户授权任务，不计为本轮发布证据。

### 9.2 P1 已证实根因与修复

1. **多酒店聚合可能越过单店事实边界**
   - 运营分析现在必须有一个明确选中的、当前账号有权访问的酒店。
   - 未选择酒店、选择多个酒店或选择未授权酒店时 fail closed，不再聚合后伪装成单店结论。

2. **旧 PMS 记录可能以不完整证据进入收益链**
   - PMS 门禁现在要求酒店身份、业务日期、来源、保存/精确回读、采集策略和核心房费对账同时成立。
   - 旧记录缺少证据时保留精确原因，不再折叠成泛化的 `read_failed`，也不允许补写字段把历史记录伪装成新证据。
   - 当前旧记录明确缺少：房费汇总明细回读、业务模块标记、来源方法、来源追踪 ID、已验证采集策略。

3. **OTA “有数据”可能被误当成“可进入收益分析”**
   - OTA 就绪条件补齐保存/回读、来源追踪和平台酒店身份要求。
   - 当前酒店的结果只由当前酒店、当前日期的适用平台决定；其他酒店缺失不会再阻塞本店。

4. **Revenue AI 默认日期和差距导航可能指向错误工作面**
   - 默认业务日期优先使用当前明确可用的业务日期，不再停留在旧请求日期。
   - 定价差距进入 `智能体中心 → 收益管理 → 定价建议` 并加载定价工作台；PMS 证据差距进入“PMS经营数据”。
   - 规范化三源事实层生效时，清除了重复、过期的 OTA 次级警告，只保留真实阻塞。

5. **PMS 阻塞时双 OTA 已验证事实曾被整体隐藏**
   - 已验证的携程、美团 OTA 事实继续显示。
   - PMS 和跨源指标保持 `null`；三源计算保持 `blocked`。
   - OTA 证据不提升为全酒店收入、入住率或完整经营事实。

### 9.3 P2 已修复的房型价格保护逻辑

- 房型配置增加显式启用/停用字段，已停用房型可以重新启用。
- 保存改为表单提交，并使用浏览器原生约束要求基础价、最低保护价、最高保护价大于 0。
- 缺失价格显示为 `--`，不再显示成 `¥0`；真实库存数量 `0` 仍保留为 `0`。
- 补充 3 条静态回归：状态切换、正数价格约束、缺失价格语义。

### 9.4 当前事实、决策与阻塞

#### 当前页面验证范围：酒店 80 / 2026-07-30

- 携程与美团 OTA 事实保持可见。
- OTA 渠道合计收入：`¥4,354.39`
- 订单：`3`
- 间夜：`5`
- OTA ADR：`¥870.88`
- PMS 和跨源指标：`null`
- `revenue_analysis=blocked`
- 唯一规范化阻塞：`dingdandao_pms_not_readback_verified`

以上数字只代表已验证的 OTA 渠道范围，不代表全酒店营业收入。

#### 独立日期复核：酒店 80 / 2026-07-29

- 携程：`readback_verified`
- 美团：`readback_verified`
- PMS：`not_verified`
- 当前酒店 OTA P0 门禁：`ready`
- 三源事实层：`blocked`
- 运营闭环：`not_closed`

正确的下一步不是补写旧记录，也不是继续补前端默认值，而是完成一次符合当前证据合同的新 PMS 采集并精确回读。只有 PMS 门禁通过后，才应继续判断最低保护价等后续经营输入是否仍缺失。

### 9.5 本轮收口验证

| 验证项 | 结果 | 说明 |
|---|---:|---|
| 高风险 PHP 目标集 | 89/89 通过，612 assertions | 收益事实层、运营管理、PMS 采集/集成、P0 执行计划 |
| Revenue AI + 房型前端回归 | 45/45 通过 | 包含 3 条新增房型价格保护回归 |
| 前端模板一致性 | 通过 | 60 个片段；模板快照和 pin 一致 |
| Tailwind 运行产物 | 通过 | 无动态类遗漏，无缺失选择器 |
| 公共入口与性能守卫 | 通过 | 入口、登录关键 CSS、公共入口和预算守卫全部通过 |
| 本地健康检查 | HTTP 200 | `http://127.0.0.1:8080/api/health` |
| 实际 served asset | HTTP 200 | 页面引用 `app-render.min.js`；房型状态、正数约束和 `--` 缺失态均存在 |

构建预算仍有两个已知 P2 风险放大器，本轮没有通过抬高阈值掩盖：

- `app-render.min.js`：1,424,588 bytes；硬上限 1,425,000 bytes；余量 412 bytes。
- 启动资源 gzip：641,898 bytes；硬上限 650,000 bytes；余量 8,102 bytes，仍处于 warning。

### 9.6 尚未解决或未验证

1. **真实 PMS 三源闭环未完成**：需要新的受信 PMS 采集、同店同日保存和精确回读；旧采集记录不能回填冒充。
2. **真实企业微信送达未验证**：本轮没有发送消息，也没有企业微信业务成功回执。
3. **生产定时器未验证或启用**：代码与本地守卫通过不等于生产计划已启用。
4. **部署、PR、CI 未执行**：本轮结论是共享本地工作区的代码、测试和 HTTP 证据，不是发布证据。
5. **资源余量偏窄**：后续新增前端模板或启动依赖时，应优先拆分/延迟加载并保持现有硬门禁，不应直接抬高预算。

本轮增量修复和验证在这里停止，不扩展新功能。真实业务闭环的唯一首要阻塞是新的 PMS 可信采集与精确回读。
