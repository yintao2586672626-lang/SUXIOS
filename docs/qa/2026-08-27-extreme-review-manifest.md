# 宿析OS 2026-08-27 极度详细复审评估合同

## 1. 目标与用户

- 目标：审查当前 GitHub 主线在代码正确性、运行稳定性、规则可执行性和架构演进风险上的重大问题，并在不扩大业务权限的前提下直接修复已证实的高影响缺陷。
- 目标用户：宿析OS管理员、酒店运营人员、收益管理人员，以及维护 OTA 数据到运营管理链路的开发者。
- 主要用户价值：真实 OTA 数据、收益分析与运营管理在同一租户、酒店、平台、业务日期和证据范围内稳定协作；失败必须可见、可恢复且不可伪装成成功。

## 2. 基线与范围

- 内部基线：`origin/main@819e90812abc99af8be636c47b4b82c615668534`。
- 基线时间：2026-08-27，Asia/Shanghai。
- 隔离工作树：`.review-worktrees/extreme-review-20260827`。
- 审查范围：`app/`、`route/`、`scripts/`、`resources/frontend/`、必要的 `public/` 入口与生成物、`tests/`、`rules/`、`hooks/`、CI 与迁移注册表。
- 默认排除：`storage/`、`runtime/`、`output/`、`reports/` 原始 OTA 捕获、`node_modules/`、`vendor/`、凭证、浏览器会话、真实 Cookie/Token、历史归档和不属于当前主线的旧 worktree。
- 允许写入：仅当前隔离工作树中的源码、测试、规则和本次审查文档。
- 禁止动作：不 commit、push、建 PR、部署、执行真实 OTA/PMS/企微写入、代替用户审批、读取浏览器凭证或清理原始脏工作区。

## 3. 值得做门

1. 当前未解决的决定：PR #40 合并后，是否仍存在会破坏主业务链、让失败被误报为成功、导致跨范围数据或使后续修改不可控的重大缺陷。
2. 会改变决定的新证据：当前主线上的可复现失败、入口/保存/回读不一致、规则与实际 gate 漂移、异常恢复缺口、显著 N+1/同步阻塞、循环依赖或生成物身份缺口。
3. 最佳候选：只审查当前主线，不重新叠加历史分支；先运行项目自己的确定性门禁，再对未覆盖的高风险路径做定向代码与故障序列检查。
4. 当前证据不足以直接停止：CI 绿色证明已登记合同通过，但不证明未登记路径、生产部署或现场结果完整，因此继续复审成立。

## 4. 标杆与有效性

- 同任务内部标杆：PR #40 合并提交、其 CI、项目现有 canonical integration gate、2026-08-25 审查与修复合同。
- 外部产品比较：`BENCHMARK_MISSING`。本轮不宣称优于其他 PMS/OTA 产品，也不授予 `comparative_pass`。
- 权威约束：PHP 8.2/Node 实际运行行为、ThinkPHP 项目现有数据与路由合同、仓库 `AGENTS.md`、`rules/`、注册迁移与当前 GitHub CI 配置。
- 有效性要求：测试必须覆盖真实失败机制而非只匹配字符串；历史结果只用于定位，不替代当前运行；生成物通过必须同时核对源码和入口身份。

## 5. 预声明评估维度

1. **代码正确性**：空值、类型、时间、幂等、事务、异常、权限、租户/酒店/平台/日期身份和保存回读是否一致。
2. **稳定性与恢复**：超时、崩溃窗口、重试、租约、任务状态、进程回收、缓存/指纹、数据库异常分类和部分结果是否可恢复。
3. **规则执行**：AGENTS、rules、hooks、package scripts 与 CI 是否引用同一门禁；规则是否真的阻止对应风险，而不是只写在文档里。
4. **架构风险**：热点文件、循环依赖、隐式 trait 合同、同步长请求、N+1、双实现、生成物身份和跨域职责是否放大变更失败概率。

## 6. 硬门

- 不泄露凭证，不跨租户/酒店，不增加未鉴权写入，不破坏真实数据。
- OTA 渠道事实不得扩大成全酒店事实；缺失、冲突、旧日期和失败不得用 0、默认值或旧数据掩盖。
- pending approval 不得被自动批准或执行；真实外部动作不属于本轮。
- 注册迁移不可改写；新 DDL 必须追加并保持幂等、checksum 和 `db:check` 一致。
- 前端源码、模板、生成物、入口指纹和实际服务身份必须一致。
- 已确认 P0/P1 与既有主业务回归不得退步。

## 7. 能力集、回归集与测量

- 基线能力：当前 canonical integration、P0 guards、public entry、source hotspot、路由覆盖、迁移检查、全量 PHPUnit、全量 Node。
- 定向能力：对新增发现建立最小复现；修复后执行失败项、修改项及直接依赖。
- 最终回归：若涉及核心多模块或门禁实现，运行一次完整 PHP/Node 与 canonical integration；未变化的已通过证据不重复执行。
- 测量：确定性命令退出码、测试/断言数、失败文件、HTTP/API 状态、SHA-256、查询次数或可复现故障序列；性能结论必须有量级证据。
- 噪声与容差：确定性合同不设主观容差；时延只在同环境重复样本足够时判定，否则保留 `INDETERMINATE`。

## 8. 判卷与复审

- 代码、测试、数据库、入口和身份由确定性 grader 判定，任何硬门失败不得被模型意见覆盖。
- 三个独立只读审查视角分别检查代码/稳定性、规则/测试、架构/性能；它们只提供待主控复现的候选，不直接授予通过。
- 只有无法规则化且会改变高影响取舍的事项才使用开放式判断；同模型意见视为相关证据，不当作独立事实。
- 最终分层只允许：`UNIT`、`INTEGRATION`、`COMPARATIVE`、`FIELD`。没有生产/用户证据时不得写 `field_validated`。

## 9. 停止与升级条件

- 所有当前可复现 P0/P1 已关闭并有防回归证据；重大 P2 已修复，或有明确的外部依赖、量级证据缺口和后续验收方法。
- 连续两轮没有可测改进、同一缺口出现三次、grader 无效或需要新外部权限时停止/升级，不用放宽合同制造通过。
- 本轮最高可授予本地 `integration_pass`；GitHub、部署和现场成熟度必须单独验证。

## 10. 证据局限

- 当前没有真实 OTA/PMS/企微写入授权，也不做生产 A/B 或酒店现场验收。
- 干净主线可做本地、CI 等价和隔离运行验证，但不能证明线上已部署相同产物。
- 外部产品级比较标杆缺失，因此比较性结论保持 `BENCHMARK_MISSING`。
