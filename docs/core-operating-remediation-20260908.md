# 核心数据与运营链后续修复

日期：2026-09-08，Asia/Shanghai。范围延续 `core-structure-optimization-20260908.md`，重点处理任务看不见、旧入口失效、数据断档与本机调度遗留。

## 结果与证据层级

本地实现、针对性测试和登录后页面检查通过。任务页“全部有权限门店”已能读出 37 条现存流程；12 条系统分析记录没有人工执行/复盘按钮；6 条被降级为未验证的历史终态记录不再显示无法提交的复盘入口。任务 #1 的观察中复盘入口仍保留。

这是现有记录的正确读取与展示，不代表新增了 37 个运营成果。核心库仍有 25 个任务，其中 12 个是 `analysis_only`、13 个是 `manual`；管理复盘和收益效果复盘目前均无持久化记录。本次未生成经营事实、代办审批或对外执行。

工作目录为 `.active-worktrees/HOTEL-clean-20260901/`。修改前基线：分支 `codex/save-all-20260903`，HEAD `9fa781fc92baa66b410f4e5d6e8c8c3c1ebae81b`，上游同名远端分支；本地 `origin/main=7cec7c2cb22694457652e3b3e0f9771eed15704e`，PR #43 为 OPEN。保留其他任务的并发修改，未提交、推送、合并或部署。

## 已修复的问题

| 问题 | 原因与修改 | 验收结果 |
|---|---|---|
| 全部门店显示成首页门店的空列表 | 通用请求助手给 `/operation/` 自动附加首页门店、租户和渠道。任务、跟踪、闭环总览、目标学习、经营记忆读取现在显式使用本页选择范围；服务端权限规则不变 | 改前全部范围无任务、单店有 5 条；改后全部范围实际读出 37 条 |
| 切店时旧行滞留；辅助脚本失败未进入错误处理 | 发起新请求时清除旧范围模型；将辅助脚本加载纳入错误处理，继续保留请求序号、登录会话与页面状态校验；拒绝缺少 list 的成功响应 | 跨店、旧请求晚到、退出登录、辅助脚本失败、畸形返回均有行为测试 |
| 经营记忆保存和回读仍受首页门店影响 | 从任务记录提取酒店身份，同一身份用于保存与按 ID、任务、酒店、层级、摘要精确回读 | 原测试复现酒店 77 取代目标酒店 7；修复后通过。写入测试使用模拟 API，未向真实库添加记录 |
| 延迟加载后的新规则没有连接到已挂载页面 | Vue setup 返回了加载前的函数引用。改为在模板调用时转发到当前辅助函数，覆盖审批、执行证据、节点检查、复盘、来源标签与学习结论 | 真实页面 12 条系统分析记录全部没有操作按钮；正常观察中任务仍可进入复盘 |
| 历史终态记录被降级展示后出现无效复盘入口 | 可操作性原来只检查展示 status，忽略保存的 reported_status。现在尊重已保存的 success/near_success/failed 终态 | 真实页面 6 条 unverified 历史终态记录均无复盘/读取事实按钮；不改写原记录 |
| 源码更新后浏览器仍用旧辅助脚本 | 延迟脚本的内容版本没有进入现有构建流程。构建自动计算 operation-static.js 哈希、更新加载版本，并检查编译期间源文件是否变化；入口校验同时拒绝旧哈希 | 浏览器实际加载 `operation-static.js?...-hfd77d4ebde`；现有内容哈希合同通过 |
| 空流程误称无瓶颈；历史策略明细占满页面 | 未读取/无流程分别显示不能判断；策略明细默认折叠，显示当前已读条数；效果说明优先采用当前证据复核结果 | 真实页面明细默认折叠；保留历史访问。暂无记录提示不再与读取错误同时显示 |
| 本机任务仍指向冻结目录 | 严格匹配两个已有任务的已知无凭证执行参数后，仅把执行路径和工作目录替换为当前入口 | 两个任务的开关、下次运行时间、最近运行时间保持一致，修复未启动任务 |

## 调度修复与真实数据现状

只修改了两个已存在任务的执行路径，没有注册新任务：

- `SUXIOS OTA Dispatcher H80`：保持 Disabled。原 `scheduler_scope_mismatch` 已解除，`action_verified=true`、`scope_verified=true`、`can_enable=true`。当前原因是 `scheduler_disabled_catch_up_enabled`；现有启用接口会执行安全启用转换。本次未启用、未采集。最后运行仍为 2026-08-11 09:54:54，结果码仍为 78。
- `SUXIOS Daily T1 OTA Room Nights H80`：保持原有 Ready 状态和计划时间，改从当前入口运行。该任务只做历史数据上的预测与实际观察，其成功不证明新 OTA 数据已取得。本次没有立即运行，也没有扩大酒店或执行人范围。

本机数据库聚合复核：

| 核心环节 | 当前证据 | 结论 |
|---|---|---|
| OTA 历史日终 | `online_daily_data` 共 85,394 条。historical_daily 的携程 business/traffic 最近至 08-31，美团 business/traffic/traffic_analysis 最近至 09-01 | 09-07 的目标日事实仍缺失；未来在手/预测及实时快照不补这个缺口 |
| 身份关联 | OTA 记录覆盖 61 个内部门店、40 个租户；缺失内部门店关联 0、租户关联不匹配 0 | 此项仅证明已检查关联，不等于所有权限场景现场验收 |
| 采集计划 | 3 个计划都在同一门店；1 个 active/enabled/ready，2 个 draft/disabled/blocked；7 个历史运行均 blocked，最近业务日 08-10 | 数据库计划启用和 Windows 调度运行是两层状态，不能以其中一层替代另一层 |
| PMS 绑定 | 订单来了配置 11 条、启用 1 条；美团云 PMS 配置 10 条、启用 0 条 | 只有一家门店启用了 PMS，不能把配置行数当作接入覆盖 |
| PMS 事实 | 订单来了 capture 13 条、1 家门店，最新业务日 08-29；美团云 PMS capture 0 条；经营日记录及快照各 6 条 | 未形成当前日、跨门店的全酒店事实覆盖 |
| 执行与复盘 | 37 个意图、25 个任务、26 条执行证据；`operation_action_reviews=0`、`operation_effect_reviews=0`、`hotel_operating_cycles=0` | 没有真实经营效果闭环验收结果；系统分析与人工经营执行必须区分 |

管理观察复盘与收益目标复盘使用不同表。观察型任务没有数值目标时，不能为了填充 effect_reviews 强行生成收益结论；本次保留这一约定，并补齐审计对两张复盘表的分别计数。

## 与上轮八项问题的衔接

1. 数据断档：修复了试点本机调度目录不一致；采集仍禁用，当前业务日事实未补齐。
2. 最大日期误导：保留按 historical_daily 和已完成日期选择历史的约束，未把未来记录当作新数据。
3. PMS 覆盖：进一步查明只有 1 个启用绑定；未替用户变更真实绑定或获取凭证。
4. 执行到效果缺证：解决范围、旧按钮、缓存和记忆保存范围问题；真实效果记录仍为 0。
5. 历史身份与质量缺失：保留原数据和来源门禁；历史终态不能通过新 UI 被随意覆盖。
6. 表/服务重叠：保留上轮冻结路由与专项工具精简；明细默认收起，未按空表或名称推测删除依赖。
7. 代码职责集中：修复当前读取与构建连接，不为缩短文件而重写 OTA/PMS 主链；大文件维护负担仍在。
8. 生产/现场证据：当前仍为本机 `development_fallback`，`production_runtime_ready=false`。没有新的生产发布或现场采集/经营效果证据。

## 验证

- 运营页面、范围恢复、保存/回读、历史终态、模板延迟绑定：73 项 Node 测试通过。
- 前端入口构建与自动辅助脚本版本：5 项 Node 测试通过。
- 现有公开页与运营入口合同：8 项 Node 测试通过，包含 operation-static 内容哈希要求。
- PMS 绑定、收益事实、效果复盘、经营问题执行桥、执行身份、Windows 调度控制：83 项 PHP 测试、763 个断言通过。保存与效果测试使用隔离 SQLite/模拟环境，不是现场结果。
- 模板同步/构建、前端入口构建、辅助脚本构建及相应校验通过；生成产物均由正式脚本写入。
- 实际管理员页面：全部范围 37 条；巢湖测试单店只显示 #7、#6、#4、#2、#1 共 5 条，没有加载中或错误空状态；12 条系统分析无操作按钮；6 条历史未验证终态无复盘按钮；观察中任务 #1 仍保留复盘；策略明细默认折叠。最终留在全部有权限门店视图。
- `/api/health` 为 ok。静态入口和运营辅助脚本的实际服务内容与本地构建 SHA-256 对照通过：入口前缀 `c80e1de18f`，运营辅助脚本前缀 `fd77d4ebde`。PHP 审计脚本语法与修改差异检查通过。最后聚合回读仍为 85,394 条 OTA、37 个意图、25 个任务，两张复盘表均为 0。

证据目录：`runtime/codex/core-operating-review-20260908/`。包含聚合审计、变更前目标文件快照、失败复现、测试结果、构建/校验结果、调度修复回执和运行检查。目录中的修复脚本只接受两个已知任务及原有开关状态；默认仅检查，重复执行不会启动任务。

## 下一步与停止点

本地代码、页面和任务路径修复完成。真实数据验收还需要针对明确的门店、日期、渠道，在原设备有效登录与明确采集/导入授权下取得当前事实，再按现有人工确认流程完成一个任务的执行证据和复盘。当前没有这些新事实，不能声称系统已经连续运营或带来收益；本轮停在此处。
