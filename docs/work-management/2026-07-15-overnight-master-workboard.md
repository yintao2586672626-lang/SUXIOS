# 宿析OS夜间自主推进总台账

> 日期：2026-07-15（Asia/Shanghai）
> 维护者：当前主控任务
> 状态：本轮可自主推进范围已收口；待用户决策见第 8 节
> 原则：无需用户确认的工作持续推进；真实授权、不可替代的业务选择和外部操作统一在最后列出。

## 1. 本轮目标

在不覆盖并行改动、不伪造 OTA 数据、不扩大业务口径的前提下，协调同一工作区内的活动任务，优先完成能够本地实现、保存、回读和验证的功能闭环。

停止条件：各活动任务完成当前已承诺的最小闭环或到达真实外部阻塞；主控完成冲突复核、集中验证和末尾确认清单。

## 2. 统一验收标准

- 功能有明确入口或可执行命令，用户能够找到并操作。
- 真实数据、派生数据、未验证、缺失和采集失败明确区分。
- 新增数据或状态覆盖保存、回读、兼容和失败阶段。
- OTA 渠道事实不扩大为全酒店经营事实。
- 不自动写 OTA，不用默认 `0`、空数组、旧数据或模拟数据掩盖缺失。
- 并发任务不覆盖、回退或重写其他任务的未提交改动。
- 每条完成声明附最小相关验证；外部状态未验证时保留为阻塞。

## 3. 活动工作流

| 工作流 | 当前目标 | 已知进展 | 下一验收点 | 状态 |
|---|---|---|---|---|
| 统一时间语义 | 过去事实、今日快照、未来预测、到期复盘 | 完整闭环及默认看板已完成；104 个测试/2,086 断言、隔离页面 1/1、健康 200；临时门店级联 37 行及账号/缓存/日志残留 0 | 无；后续仅在新需求触发时继续 | 已完成 |
| 管理员安全监测 | 管理员查看高频登录、危险操作及账号登录活跃度 | 活跃度终审 PHP 29/167、Node 3/3、真实页面 1/1；1/7/30 天真实统计分别为 26/7/19/4、26/20/6/4、26/20/6/4；名单无 IP，临时残留 0 | 用户醒后决定是否采取权限、抓取或代理配置动作 | 已完成 |
| 携程经营闭环 | 携程真实数据到收益、AI、运营和复盘 | 两次期别窄迁移合计 367 行、酒店 80 为 87 行；`#43491` 已按去哪儿竞对排除，报告 #4 只引用本店过程快照 `#17652`，纠正链不写 OTA | 自动每日调度、2026-07-16 次日效果与 ROI 仍缺真实运行证据 | 本轮收口；自动闭环未完成 |
| 非采集主链修复 | 可信收益、AI到运营契约、真实状态回报 | 已完成缺字段 422/不落伪记录、酒店范围回读、intent/task 严格按 ID 回读、CAS 状态转换及日报时间/平台/缺失值语义修复 | 无；真实经营输入仍按各 OTA 证据门禁处理 | 已完成 |
| 美团经营闭环 | 敦煌漠蓝新真实采集、诊断、人工执行和复盘 | 最终有效记录 `#212 / blocked_by_data`：核心指标缺失、8 项证据缺口、无可执行动作、无 OTA 写入；`#210 → #211 → #212` 替代链完整 | 需要用户重新完成美团 Profile 登录，或改用下一个真实可采营业日 | 本地闭环完成；外部登录阻塞 |
| 4000 用例与 P0 实测 | 数据驱动组合测试、缺陷修复、证据账本 | canonical 账本为 `144 partial + 3856 not_executed`；P0 `144/2688` 已留证、`2544` 待执行；TC-387/TC-404 已修复并重放 | 优先补本地可执行 P0 证据，再等待真实 OTA/生产外部证据 | 本轮修复收口；证据继续补齐 |

## 4. 已完成的协调动作

- 已读取同项目最近任务列表及六个活动任务的当前回合。
- 已向六个活动任务下发统一指令：自主推进、确认项末尾汇总、写共享文件前复核并发差异。
- 已确认两个低风险独立成果：知识中枢映射层拆分已完成定向等价验证；AI 微信群运营机制方案已完成，不再占用项目代码。
- 已确认当前 `HOTEL/` 工作区存在大量并发未提交改动，主控不在未分配的共享业务文件上追加修改。

## 5. 当前共享冲突区

以下路径由多个活动任务触碰或即将触碰，写入前必须重新读取 diff；主控默认只做协调与验证：

```text
app/controller/Agent.php
app/controller/Auth.php
app/controller/concern/OnlineDataHistoryConcern.php
app/service/AiDailyReportService.php
app/service/OperationManagementService.php
app/service/RevenueResearchService.php
app/service/OtaStandardEtlService.php
route/app.php
public/index.html
public/app-main.js
resources/frontend/templates/fragments/
tests/AgentTest.php
tests/OnlineDataTest.php
tests/OperationManagementServiceTest.php
tests/RevenueResearchServiceTest.php
package.json
vault/project-state.md
```

## 6. 主控检查点

- [x] 建立持续目标并盘点任务。
- [x] 建立总台账。
- [x] 下发自主推进与末尾确认规则。
- [x] 读取第一轮并行任务完成/阻塞状态。
- [x] 复核共享文件是否发生覆盖、冲突或口径回退。
- [x] 运行与稳定改动相匹配的集中验证。
- [x] 更新 `vault/project-state.md` 中的已验证事实。
- [x] 输出最终完成项、未验证项、外部阻塞与末尾确认清单。

## 7. 进度日志

### 2026-07-15 初始检查点

- 本次 `/goal` 已激活，无显式 token 或时间上限。
- 同项目共读取到 10 个任务：当前主控 1 个、活动业务/测试任务 6 个、已完成或空闲任务 3 个。
- 当前策略是让原任务继续拥有自己的业务范围；中央台账只协调边界、验证与最终收口。

### 2026-07-15 第一轮滚动检查

- 六个活动任务均继续运行，并已接收“自主推进、末尾确认、共享文件写前复核”的指令。
- 当前稳定增量：可信收益门禁相关 55 个后端测试和 2 个静态守卫通过；4000 用例账本累计 96 条直接证据。
- 当前待定位红灯：完整 PHP 回归 1,486 项中 1 项失败，涉及广告效率/服务质量在数据可信度阻断时的旧测试期望；尚未把它判成产品回退。
- 当前并发快照再次执行 `git diff --check`，未发现冲突标记或空白错误。

### 2026-07-15 第二轮滚动检查

- 管理员安全监测已形成只读后端聚合和页面源码，前端编译成功；仅剩共享入口中的构建哈希同步及页面验收。
- 4000 用例全量 PHP 单失败已复核为运行期间文件并发写入，不需要回退生产可信度门禁；相关测试当前 32/32 通过。
- 美团闭环已增加旧诊断作废/可追溯方向，待真实 Profile 重跑；任何登录失效会保留为数据阻塞。

### 2026-07-15 第三轮滚动检查

- 时间语义工作流已完成带备份的窄迁移：361 条美团未来流量预测不再冒充定稿事实；预测版本表已创建并回读到 26 个字段。
- 已向美团闭环任务发出跨流提醒：重跑前必须读取迁移后的数据库，确认未来预测不再进入目标日核心经营事实；若同为酒店 80，要把影响纳入验证证据。
- 安全监测的代码、权限、构建与入口契约均已通过，当前进入实际后台权限和页面验证。

### 2026-07-15 第四轮滚动检查

- 已确认酒店 80 就是敦煌漠蓝新；美团闭环接受迁移结果，并承诺把 361 条未来预测与 7 月 14 日核心经营事实隔离作为真实验收证据。
- 酒店 80 当前只有 1 天收入/订单定稿事实，时间语义工作流不会硬凑收入预测；首版未来趋势仅输出证据足够的曝光、访问和提交，其他指标标记样本不足。
- 安全监测因本机缺少 Python Playwright 改用项目现有 Node Playwright，不安装新依赖，并使用一次性本地令牌验证后清理。

### 2026-07-15 第五轮滚动检查

- 时间语义真实保存闭环已成立：酒店 80 生成 21 个流量预测点，保存与回读均为 21；样本不足指标未生成预测。
- 非采集主链正在消除数据库层与报告层的二次日期筛选，目标是只保留一个范围真相，并把“列表截断”和“总数未知”分开。

### 2026-07-15 第六轮滚动检查

- 发现业务链报告的跨酒店统计风险：未传 `system_hotel_id` 时旧逻辑会混合所有酒店运营记录，却声明为“同酒店”。修复方向是无门店即不加载运营统计并显式报告范围缺失。
- 已同时通知携程、美团闭环：后续验收必须显式传当前门店 ID，并按最新报告契约复验，不依赖无门店参数的旧统计。
- 工作区再次通过 `git diff --check`，仍无冲突标记。

### 2026-07-15 第七轮滚动检查

- 美团旧诊断作废/可追溯闭环已通过代码级后端与前端测试；旧记录保留历史证据但禁止再次转运营执行。
- 安全监测页面验收不使用现有管理员密码；将创建随机临时本地超级管理员，验收后删除临时账号、会话及其测试日志，不覆盖现有用户。
- 携程、美团均已接收单门店范围要求；美团后续固定使用 `system_hotel_id=80`。

### 2026-07-15 第八轮滚动检查

- 实时快照从小时覆盖调整为分钟级可回看版本；新增/同分钟更新将用事务临时行验证并回滚，只有回读一致才算保存成功。
- 旧“美团 AI 分析”已确认选择竞对 POI，不具备本店口径；将降级为未验证竞对预览、关闭经营/定价建议。已通知美团闭环不得依赖该入口生成本店行动。
- 4000 用例第五批发现 4 个真实点评缺陷：个人信息脱敏不完整以及 stale/失败/缺字段质量状态被误标正常；已进入最小修复。

### 2026-07-15 第九轮滚动检查

- 安全监测真实浏览器验收已通过，且临时隔离身份及测试日志已清理；现有账号与密码未改动。
- 截图复核发现顶部说明区浅色背景配白字，对比度不足；只修该区，不调整其他并发页面。

### 2026-07-15 第十轮滚动检查

- 时间轴与安全监测同时重建共享前端产物，造成 `public/index.html` 内容哈希来回过期；代码内容未丢失，但最终守卫被并发窗口干扰。
- 已串行化所有权：本窗口由时间语义任务执行最后一次共享前端构建与哈希对齐，必须保留安全监测片段；安全监测任务停止写入口哈希，只在最新产物上复验自身功能。
- 点评隐私修复已通过 8/8 L8（72 断言）及 9 个相邻回归（87 断言）。批量导入原子性已通过 8/8 L8（104 断言）和 80 个同步服务回归（562 断言）；失败整批回滚、原始证据与失败任务保留、重试得到 64 条唯一记录。

### 2026-07-15 第十一轮滚动检查

- 携程闭环与非采集主链同时写入 `app/service/OperationManagementService.php`，已主动串行化：本窗口由非采集主链完成状态真相、门店隔离和竞对预览降级；携程任务停止继续写服务文件，先保留边界测试，待文件稳定后重读并只补缺失。
- 禁止任何一方回退对方已落入共享文件的增量；稳定后需要同时通过通用运营回归与携程本店事实边界测试。

### 2026-07-15 第十二轮滚动检查

- 运营服务串行化复核捕获第 2777 行合并语法断点；当前文件所有者仅修该断点、保留携程边界增量后重跑完整回归。
- 第五批测试已正式落账：总计 120/4000 条 `partial` 直接证据，未把本地服务/SQLite证据包装成全链路通过。
- 美团单店账号无超级管理员权限导致 Agent 中心页面被拒绝；已下发一次性隔离超级管理员方案，页面验收后必须清理所有临时身份与日志，不再等待用户密码。

### 2026-07-15 第十三轮滚动检查

- 共享前端三哈希已锁定：Tailwind `1d29e2540c`、模板渲染 `fd71205fb7`、入口 `109cda0682`；安全监测前缀与片段均保留。已通知安全监测开始最终只读复验。
- `OperationManagementService.php` 已由当前所有者修复语法断点并冻结；117 个 PHPUnit、4 个业务链报告测试、11 个前端/静态守卫通过。已通知携程任务恢复边界复核、测试任务重跑全量。
- 4000 用例全量回归曾因并发语法断点连锁失败 51 项；该断点现在已消失，禁止重复修复，仅复核仍存在的单个边界失败和方法缺失。
- 主控独立复核：`C:\xampp\php\php.exe -l app/service/OperationManagementService.php` 通过，`git diff --check` 通过，冲突标记为 0。

### 2026-07-15 第十四轮滚动检查

- 携程边界与4000用例都收敛到 `OnlineDailyDataPersistenceService::applyPeriodFields`，同时该文件包含时间语义分钟快照增量。
- 已串行化：4000用例任务是该方法唯一修复者，必须保留分钟快照和保存回读逻辑；携程与时间语义停止写该服务，修复后只跑各自边界回归。
- 验收要求：`OnlineDailyDataPersistenceServiceTest`、`OtaClosureDataBoundaryTest`、时间语义测试与全量回归同时通过后才冻结文件。

### 2026-07-15 第十五轮滚动检查

- 唯一所有者已修复周期边界并通过 89 个相关测试、604 断言：误标 historical 的当日数据纠正为 realtime snapshot，未来流量预测纠正为 next_30_days；运营文件未再修改。当前正在重跑全量 PHP。
- 携程迁移已在代码冻结指令后暂停，dry-run 仅统计未写库：全库 274 条当日实际数据需改期别，其中酒店 80 为 33 条；业务数值与来源证据不在拟修改范围。
- 时间语义页面隔离验收发现新预测表未纳入门店级联清理；正在补 `temporal_forecast_snapshots` 删除与回归断言，避免临时/真实门店删除后残留预测版本。

### 2026-07-15 第十六轮滚动检查

- 全量 PHP 已达到 1,528 tests / 15,284 assertions / 0 failures / 0 errors，但主控源码复核发现 `applyPeriodFields` 仍存在两段连续的等价/冲突周期判断。
- 已把精确行段交回唯一所有者：保留 190-196 行的规范化、多类型、未来日期逻辑，删除 197-203 行的重复无条件 `traffic_forecast` 分支；清理后必须重新跑边界、携程、时间语义与全量测试。
- 因此 1,528/15,284 暂作为清理前基线，不提前放行携程 274 条真实库迁移。

### 2026-07-15 第十七轮滚动检查

- 时间语义首轮隔离数据链成功：临时门店保存并回读 15 条事实与 21 个预测点；临时门店、预测、账号、2 个登录缓存和 3 条登录日志均清零。
- 页面验收发现组件只挂在旧 `compass` 视图，默认 `ai-workbench` 没入口；将复用同一数据组件补到默认看板顶部，不复制预测逻辑、不重排核心结构。
- 周期唯一所有者已接受重复块清理，暂停第六批；清理完成后重跑边界、分钟快照及全量 PHP。

### 2026-07-15 第十八轮滚动检查

- 周期重复块已删除；清理后 89 项边界/604 断言、携程边界 5 项/24 断言、分钟快照专项 1 项/3 断言通过，正在再次跑全量。
- 美团真实重跑 #211 已诚实停在数据不足：核心指标未返回、0 可执行动作、1 个补数阻断、未创建运营执行单；#210 不再作为有效闭环。
- 美团页面仍有“收益分析证据充分”的过度乐观文案，正在按真实缺口修正。

### 2026-07-15 第十九轮滚动检查

- 非采集主链本阶段已稳定完成：PHPUnit 117/117（2,275 断言）、Node 15/15、语法与差异检查通过；酒店 80 单店统计与 P0 阻断传递正确，无门店时运营统计保持 `null`。
- 新一段 AI→运营契约只允许先做只读审计和失败测试；在携程/周期/美团共享链收口前，锁定 `OperationManagementService.php`、`Agent.php`、`AiDailyReportService.php`、`route/app.php` 与共享入口，暂不写入。

### 2026-07-15 第二十轮滚动检查

- 重复周期块清理后的完整 PHP 最终仍为 1,528/1,528；周期文件正式冻结。
- 已解除携程/时间语义的只读等待：携程先复跑 27 项边界，通过后才可执行带备份的 274 条元数据窄迁移；时间语义只复跑相关测试与页面清理，不再写持久化服务。
- 时间语义默认看板真实页面已显示过去/如今/未来、预测按钮与“回看当时”，无页面错误；临时事实与预测再次清零，当前做完成前守卫。

### 2026-07-15 第二十一轮滚动检查

- 时间语义工作流已完成：104 个测试/2,086 断言、真实隔离页面 1/1、健康接口 200；临时门店级联 37 行、账号 1、角色 1、缓存 2、登录日志 3 均清理为 0。
- 默认看板补入口后的最终共享哈希为 Tailwind `1d29e2540c`、Render `06b24d42ba`、App Main `109cda0682`；已通知安全监测必须以这组而非旧 Render 哈希做最终复验。

### 2026-07-15 第二十二轮滚动检查

- 管理员安全监测在最终三哈希基线上完成：PHP 26 tests/138 assertions、静态契约 2/2、Playwright真实页面 1/1、临时用户与登录日志残留 0。
- 30 日证据：VIP006 触发 505 次限流（2026-07-14 11:27–17:38）；VIP002 有 192 次受保护访问拒绝；VIP002/004/005/008 有历史成功删除携程配置，VIP004 另有历史强制删除酒店记录。
- 2,837 条 IP 观测全部为 `127.0.0.1/::1`，只能证明代理未传真实来源 IP，不能据此判断真实登录地址或主观恶意。

### 2026-07-15 第二十三轮滚动检查

- 携程窄迁移已执行：全库 274 条今日过程数据、酒店 80 为 33 条；四个时间字段各变更 274 次，`update_time` 与业务/来源字段指纹完全不变。
- 迁移后预测待修复 0、当日历史误标 0、酒店 80 异常 0；当前进入服务/API回读，核验旧竞对 268 不再进入本店诊断。

### 2026-07-15 第二十四轮滚动检查

- AI→运营隔离红灯已落地且真实失败：必填字段不应落 blocked、参数异常应为 422、intent/task 按 ID 严格回读、前端成功提示前必须回读；红灯放在 `evals/`，未污染默认 PHPUnit，共享文件未改。
- 4000 用例第六批选定 TC-038 授权到期、TC-269 提示注入抵抗、TC-407 异步任务断点恢复，先建立 24 个红灯。生产修复前必须报告文件清单；涉及当前中央锁的部分只保留证据，不得抢写。

### 2026-07-15 第二十五轮滚动检查

- 第六批生产修复范围已收窄：仅允许 TC-038 修改 `HotelScopeService.php` 并完成授权到期边界回归；TC-269 与 TC-407 只保留可重复红灯，不修改当前被锁定的 AI 日报与同步服务。
- 美团任务被指定为本轮最后一个共享前端构建所有者：必须基于最新全量源码保留时间轴、安全监测、竞对预览降级等并行增量，完成后报告 Tailwind/Render/App Main 哈希及四项入口校验并冻结产物。
- 携程任务收口必须补充迁移备份绝对路径、行数/校验方式、酒店 80 服务/API 回读、定时任务真实运行状态与次日效果证据；不能把代码具备能力表述为当前机器已持续运行。
- 主控检查时工作区共有 143 条状态变更；`git diff --check` 通过，任务范围内冲突标记为 0。此时仍处于并发写入窗口，不作为最终放行证据。

### 2026-07-15 第二十六轮滚动检查

- TC-038 授权到期已完成：等于当前秒或更早的授权立即失效，`NULL` 永久授权与未来授权保持有效；目标 L8 8/8（78 断言）、门店作用域/权限回归 47 项（257 断言）通过。
- 主控发现未修复的 TC-269 红灯曾被放入默认 `tests/`，已要求迁往隔离 `evals/`；TC-269/TC-407 不得让默认 PHPUnit 主动变红，也不得计入已完成证据。
- 携程第一次收口被证据审计退回：数据库 `online_daily_data#43491` 为 `hotel_id=-1`、`platform=Qunar`、`compare_type=competitor_avg` 的竞对行，不能作为酒店 80 的本店携程漏斗。
- 与该错误漏斗关联的 intent 54、task 28、evidence 34 是宿析OS内部手工执行记录，并非 OTA 平台写入；必须保留审计原件并追加纠正/作废链，不能继续作为本店日报或执行成功依据。
- `online_daily_data#17652` 可证明酒店 80 当日携程收入 5,939、订单 11、间夜 7、ADR 848.43，但其状态为 `realtime_snapshot / is_final=0`，只能称当日过程快照，不能称日终定稿事实；本店漏斗、OCC、RevPAR仍应明确缺失。

### 2026-07-15 第二十七轮滚动检查

- 美团闭环已诚实收口到外部登录阻塞：有效记录为 `#212 / blocked_by_data`，核心指标均为缺失、8 项核心证据缺口、无可执行经营动作、无 OTA 写入；`#210` 无效并由 `#211` 替代，`#211` 再由 `#212` 替代。
- 美团页面真实验收通过：`#212` 显示“未分级/数据不足”且无执行按钮，`#210` 显示“无效证据 + 已替代”；竞争圈仍展示 21 家。最终哈希为 Tailwind `1d29e2540c`、Render `3ef75ecaa0`、App Main `b5c646d75d`。
- 美团回归：PHP 76/1,742，采集门禁 21/21，每日闭环与页面 72/72，竞争圈 18/18，四类前端入口校验与健康 200 均通过。一次性管理员用户/角色/映射/登录日志/操作日志/会话残留均为 0，真实诊断保留。
- 第六批测试收口：TC-269 在隔离 `evals/` 中为 8 项/91 断言/1 失败；TC-407 为 8 项/72 断言/4 失败；均未计入完成证据。默认发现共 1,539 项，隔离红灯命中 0、TC-038 正式测试命中 8。
- 已停止测试线程自动开启第七批；等待共享树稳定后统一做中央全量与入口验证。

### 2026-07-15 第二十八轮滚动检查

- 携程错误执行链已完成可追溯纠正：intent 54=`rejected`，task 28 保留 `manual/executed` 审计但 `result_status=failed`，action 477=`finished/failed`，新增 evidence 36=`data_scope_correction` 且 `ota_write_performed=false`；原 evidence 34 未删除。
- 携程纠正后日报只引用本店 `online_daily_data#17652`，不再引用 `#43491/#15676/268`；真实页面也只显示该来源，并显示本店漏斗待接入。页面执行追踪明确“仅宿析OS内部核验、未改OTA、无次日效果或ROI”。
- 携程最终期别修复另处理过去 78 行、今天 15 行、未来 0 行（酒店80为54行）；迁移后错误数0，业务值、来源与 `update_time` 指纹不变。一次性验收 user221/role31/映射/登录日志/操作日志已由主控数据库回读为0。
- 仍发现 AI 日报将同日 `realtime_snapshot/is_final=0` 写成 `Yesterday result`；这不是携程数据问题，已交给下一位 AI→运营唯一所有者随严格契约一起修复。
- 普通门店每日诊断可达性审计确认存在“入口隐藏 + 模块未授权 + Agent仅超管 + 通用日志跨店风险”四层问题；本轮不直接开放整个 Agent 中心，只把租户隔离窄入口作为最终决策项。
- 已解除 AI→运营共享锁并恢复唯一所有者：实施 422/不落伪记录、intent/task 按ID且按酒店回读、前端严格回读后成功，以及同日快照摘要修复；该窗口将成为新的最终共享前端构建所有者。

### 2026-07-15 第二十九轮滚动检查

- 携程最终核心回归为 122/122（812 断言），差异检查通过、健康接口 200；完整每日自动闭环仍未完成，不作完成宣称。
- 两次期别迁移合计纠正 367 行，酒店80合计87行。第一次备份：`storage/backups/ota_period_semantics/online_daily_data_affected_before_20260715_113310.sql`，1,316,507 bytes，SHA256 `588655D1E2ED5B6D1D2C986BB109406744DE667AC6D9FD8BB512FC6985FD129E`。
- 第二次备份：`storage/backups/ota_period_semantics/traffic_forecast_period_before_correction_20260715_120304.sql`，250,052 bytes，SHA256 `A93CEC874FFC0D8C049D4B02146407DD0C7A5B980A1059C5D36B02CF4ACA65EF`。两份均为逐行 SQL 审计备份，只能按主键受控恢复。
- 当前机器未注册 OTA 自动采集 Dispatcher；`Daily Workbench Patrol` 只是只读巡检，`Local Stack Autostart` 最近结果仍为失败码 `3221225786`。2026-07-16 次日数据尚未产生，因此无效果复盘或 ROI 证据。
- 携程仍有两个由 AI 日报所有者处理的显示缺陷：同日快照误写 `Yesterday result`；已有本店经营快照时却笼统写“OTA数据为空”，应收窄为“本店携程漏斗缺失”，曝光/访客 0 不能解释为真实零值。

### 2026-07-15 第三十轮滚动检查

- AI→运营严格契约已完成：缺少价格关键字段返回 422 且不创建伪 blocked 记录；所有写动作成功前必须按酒店和返回 ID 回读 intent/task/evidence；审批与执行使用 CAS 防止并发状态覆盖。
- 日报 #4 已回读为 `current_day_process / 当日过程快照`，仅引用 `online_daily_data#17652`；收入 5,939、订单 11、间夜 7、ADR 848.43，曝光/访客保持 `null`，缺口收窄为“本店携程漏斗缺失”。
- intent 54=`rejected`、task 28=`result_status=failed`、action 477=`failed`、evidence 36=`data_scope_correction` 且 `ota_write_performed=false`；未生成次日效果、ROI 或投资结论。

### 2026-07-15 第三十一轮滚动检查

- 账号登录活跃度终审修复完成：超级管理员排除与 `User::isSuperAdmin()` 对齐；活跃证据改为无 10,000 行上限的 `action=login` 数据库分组聚合；logout/register/refresh 不再更新登录时间。
- 真实 1/7/30 天统计为 26/7/19/4、26/20/6/4、26/20/6/4（启用/活跃/未活跃/其中从未成功）；三档 `complete=true`、coverage 完整、来源错误为空，名单无 IP 键。
- 终审专项为 PHPUnit 29/29（167 断言）、Node 3/3、Playwright 1/1；普通账号 API 403、临时超管 API/页面 200，临时用户/角色/酒店/登录日志残留均为 0。

### 2026-07-15 第三十二轮滚动检查（历史发现快照，已由 2026-07-16 收口更新取代）

- 用户解除停止边界后，第七批仅在 `evals/seventh-batch/` 本地隔离执行：24 个唯一用例、112 断言，锁定或共享生产文件触碰 0，默认 PHPUnit 发现命中 0。
- TC-104 密文篡改 8/8 绿，但证据状态仍为 `partial`；TC-387 CORS 来源限制 8/8 红，定位到 `Cors.php` 仍硬编码 `Access-Control-Allow-Origin: *` 且无 allowlist。
- TC-404 外部依赖超时 8/8 红，定位到 `LlmClient.php` 接受 999 秒无界超时且缺少清晰的超时/可重试失败语义；本轮按授权只留证据，未修改生产实现。

### 2026-07-15 第三十三轮滚动检查（历史回归快照，已由 2026-07-16 收口更新取代）

- 中央完整回归通过：PHPUnit 1,570/1,570（15,530 断言），Node 539/539；美团/携程三个旧契约已改为明确禁止前端重新暴露原始、接口级或排障数据，没有恢复已隐藏区块。
- 四类前端守卫全部通过；最终哈希为 Tailwind `1ae7dced37`、Render `e5d6a1a498`、App Main `2ca0d6b672`，`public/index.html` 引用一致；健康接口 HTTP 200。
- 主控再次用随机自定义超管完成真实安全监测页面 1/1 验收并切换 1/7/30 天；清理后用户/角色/登录日志均为 0。中央只读数据库复验与终审统计一致。
- 最后卫生复验：`git diff --check` 通过、冲突标记 0、临时脚本 0；第七批证据重放仍为 24 例/112 断言、8 绿/16 红、共享生产文件触碰 0。

## 8. 最后统一确认（当前不阻塞）

暂不向用户提问。只有以下类型在本地工作完成后进入本节：

- 需要用户本人完成的 OTA 登录、验证码或设备授权。
- 会改变酒店、平台、日期、金额、执行范围或发布范围的业务选择。
- 自动封号、删除数据、真实 OTA 写入、对外发布等不可逆或外部动作。
- 缺少真实经营输入且任何合理假设都会改变结论的事项。

当前待用户醒后决策（均不阻塞本地功能）：

- 使用现有管理员在“OTA数据 → 美团数据”完成一次美团 Profile 重新登录验证；若平台不再返回 2026-07-14 历史核心数据，则改用下一个真实可采营业日重新生成诊断。
- 是否为获授权的普通门店账号另建“每日 OTA 诊断”租户隔离窄入口；当前不能直接开放整个 Agent 中心，因为还叠加模块授权、超管守卫和通用日志跨店风险。
- 是否启用无人值守 OTA 调度；补跑、失败重试、成功判定和 CLI/HTTP 失败返回语义已于 2026-07-16 修复，`Local Stack Autostart` 已恢复为 `Ready / LastTaskResult=0`。当前只剩用户是否启用及 Windows 运行账户选择，尚未注册 OTA Dispatcher。
- 是否暂停 VIP006 的自动抓取能力。
- 是否临时收紧 VIP002/VIP004/VIP005/VIP008 的删除与配置权限。
- 是否配置可信代理的真实 IP 转发；未配置前不把 `127.0.0.1/::1` 当成用户真实地址。

### 2026-07-16 收口更新

- TC-387 已完成本地生产代码修复：CORS 使用明确 allowlist，允许/拒绝来源契约与高风险安全门禁通过，不再列为待决策项。
- TC-404 已完成本地生产代码修复：LLM/外部调用超时与重试总时长已设上限，可重试超时和最终失败语义已有测试，不再列为待决策项。
- 调度策略、修正台账、前端数据真值、AI→运营严格契约和本地启动已完成中央回归；最终基线见 `vault/project-state.md` 的 `2026-07-16 Verified: Daily Closure and Regression Baseline`。

### 2026-07-16 昨日修改复盘终审

- 审查范围：2026-07-15 的 5 个提交（`133637f`、`d9a3125`、`8c060c9`、`0c6fe15`、`a7e262a`）及其后未收口的工作树。原终审快照有 `278` 条 Git status 记录，展开为 `284` 个常规文件（`206` tracked、`78` untracked）；现已按 OTA、运营、诊断、前端、测试和收口文档拆为 40+ 个可审查本地主题提交，未推送。
- 已关闭的数据真值问题：OTA 采集只有“保存数量大于 0 且严格回读成功”才显示入库成功；首页时间视角排除 `saved_success !== true` 的事实，并排除 `competitor_avg/peer` 等非本店流量；错误态不再保留旧卡片、旧日志统计或伪成功。
- 已关闭的运营并发/越权问题：扩张执行意图只接受服务端校验过的可信来源；通用接口不能靠客户端伪造 `readiness_stage/source_record_id` 绕过；任务复核 CAS 同时比较旧状态与旧摘要；扩张来源行在创建时加锁，`idempotency_key` 由数据库唯一索引兜底，8 个真实并发 PHP 进程收敛为 1 个意图 ID/1 行。
- 已关闭的诊断证据问题：第七批 24 个 L8 变体统一由 canonical runner 执行；4000 证据账本重建为 `144 partial + 3856 not_executed`；P0 为 `144/2688` 已留证、`2544` 待执行，gap-first 为 `64/600` 已留证、`536` 待执行。未执行项没有被包装成通过。
- 已关闭的测试/页面问题：快速 E2E 默认连接专用 `hotelx_e2e` 与 `127.0.0.1:18080`，共享库/远程库均需显式门禁；首次专库执行发现并修复 Vue 模板读取 `window.innerWidth` 的运行时错误，最终 `4/4` 通过，清理残留 `0`，未选择或迁移 `hotelx`。项目 AI 页面继续按产品范围冻结。
- 已完成全新 MySQL 验证：`database/init_full.sql` 初始化得到 `82` 张表；扩张幂等迁移连续执行两次后仍为 1 个字段和 1 个唯一索引；真实并发验证行已清理。
- 最终验证：PHPUnit `1700/1700`（`16592` assertions）；Node `573/573`（`88` files）；隔离快速 E2E `4/4`；P0、前端模板、公共入口、生产入口卫生、显示边界、保护核心、高风险安全、非安全复核、语法、冲突标记、零字节与 `git diff --check` 全部通过；本地健康接口 HTTP `200`。
- 最终前端内容哈希：Tailwind `72130868ce`、Render `ddc79dfbad`、App Main `d92b8b5c88`、System Static `b8931c46a3`，`public/index.html` 引用已对齐。
- 未在本轮宣称完成：真实携程/美团授权采集、生产部署/证明、可信代理真实 IP、次日效果/ROI、真实携程定价输入，以及 4000/P0 未执行证据补齐。这些是外部事项或证据欠账，不是已确认缺陷数。
