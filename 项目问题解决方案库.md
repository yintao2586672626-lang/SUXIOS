# 🏨 酒店管理系统 - 问题解决方案库

> 全面收录项目运行、开发、部署过程中遇到的所有问题及解决方案

---

## 🤖 自动问题追踪系统

本项目已集成 **自动问题追踪系统**，当同一问题发生 **4 次以上** 时自动归档到本库。

### 🚀 快速使用

```cmd
:: 记录问题（达到4次自动归档）
track_issue.bat "问题标题" "问题类别"

:: 示例
track_issue.bat "数据库连接超时" "数据库"
track_issue.bat "页面加载缓慢" "性能"

:: 查看统计
track_issue.bat stats
```

### 📊 当前统计

运行 `track_issue.bat stats` 查看实时统计数据。

> 📁 详细说明参见：`.issue_tracker/README.md`

---

## 📑 目录

- [🚀 运行环境问题](#-运行环境问题)
- [🔧 代码问题](#-代码问题)
- [💾 数据库问题](#-数据库问题)
- [🌐 网络/部署问题](#-网络部署问题)
- [📊 数据问题](#-数据问题)
- [🔐 权限/认证问题](#-权限认证问题)
- [⚡ 性能问题](#-性能问题)
- [🧭 当前项目重点问题归档（2026-05）](#-当前项目重点问题归档2026-05)

---

## 🚀 运行环境问题

### 问题 #001：PHP 服务器启动失败 - 找不到 router.php

| 项目 | 内容 |
|------|------|
| **问题描述** | 运行 `run.bat` 启动 PHP 内置服务器时报错：`Failed opening required 'router.php'` |
| **错误信息** | `Fatal error: Failed opening required 'router.php' (include_path='C:\xampp\php\PEAR')` |
| **问题类型** | 路径问题 |
| **根本原因** | `run.bat` 中使用 `%~dp0public\router.php` 相对路径，PHP 解析路径错误 |
| **解决方案** | 使用绝对路径启动，先切换到 public 目录再启动 |
| **正确命令** | ```cmd<br>cd /d c:\Users\Admin\Desktop\JDXM\JDSJ\HOTEL\public<br>C:\xampp\php\php.exe -S 0.0.0.0:8081 router.php<br>``` |
| **相关文件** | `run.bat`, `start.bat` |
| **预防措施** | 使用 `start.bat` 脚本启动，避免手动输入命令导致路径错误 |

---

### 问题 #002：端口 8080 被占用

| 项目 | 内容 |
|------|------|
| **问题描述** | 启动 PHP 服务器时端口 8080 已被多个进程占用 |
| **错误信息** | 无法绑定端口，或启动后无法访问 |
| **问题类型** | 端口冲突 |
| **根本原因** | 之前多次启动的 PHP 进程未正常关闭，导致端口冲突 |
| **解决方案** | 1. 换用其他端口（如 8081、8090）<br>2. 清理残留进程 |
| **验证命令** | `netstat -ano \| findstr :8080` |
| **清理命令** | `taskkill /F /IM php.exe` |
| **预防措施** | 1. 使用固定端口范围（8081-8090）<br>2. 启动前检查端口占用<br>3. 及时清理残留进程 |

---

### 问题 #003：ngrok / 临时公网隧道已禁用 【风险项】

| 项目 | 内容 |
|------|------|
| **问题描述** | 历史文档中存在 ngrok / 临时公网隧道使用说明，可能诱导把本地开发服务暴露到公网 |
| **风险信息** | 可能暴露后台登录页、Cookie 回传接口、OTA 抓取接口、调试接口或本地环境配置 |
| **问题类型** | 安全风险 / 外网暴露 |
| **根本原因** | 早期为了临时联调保留过公网隧道说明；当前项目已改为本地访问和正式部署链路，不再需要 ngrok |
| **处理方式** | 1. 不再启动 ngrok 或任何临时公网隧道<br>2. 删除文档中的启动命令和 4040 API 获取方式<br>3. 本地联调仅使用 `http://127.0.0.1:8080/` 或正式受控环境<br>4. 确需外部访问时，必须使用正式网关、HTTPS、鉴权、IP 白名单和审计 |
| **排查命令** | `tasklist \| findstr /i ngrok` |
| **预防措施** | `.gitignore` 忽略 ngrok 二进制和配置文件；禁止在文档、脚本、README 中新增临时公网隧道启动命令 |

---

### 问题 #004：历史公网隧道端点离线问题 【已废弃】

| 项目 | 内容 |
|------|------|
| **问题描述** | 历史上通过临时公网隧道访问本地服务时，曾出现端点离线 |
| **历史错误代码** | `ERR_NGROK_3200` |
| **问题类型** | 已废弃外网联调方式 |
| **根本原因** | 临时公网隧道不可控，且会扩大本地开发环境暴露面 |
| **处理方式** | 不再排查或恢复该链路；遇到外部访问需求时，改走正式部署环境或内网/VPN 联调 |
| **预防措施** | 不保留临时公网 URL，不通过聊天、文档、截图传播本地服务访问地址 |

---

### 问题 #005：XAMPP MySQL 服务未启动

| 项目 | 内容 |
|------|------|
| **问题描述** | 系统提示数据库连接失败 |
| **错误信息** | `Connection refused` 或 `无法连接到数据库` |
| **问题类型** | 服务未启动 |
| **根本原因** | XAMPP MySQL 服务未启动 |
| **解决方案** | 1. 打开 XAMPP Control Panel<br>2. 启动 MySQL 服务<br>3. 检查端口 3306 是否被占用 |
| **预防措施** | 将 MySQL 设为开机自启动，或创建启动脚本 |

---

## 🔧 代码问题

### 问题 #006：OnlineData.php 重复函数定义

| 项目 | 内容 |
|------|------|
| **问题描述** | PHP 报错：`Cannot redeclare function` |
| **错误信息** | 函数重复定义错误 |
| **问题类型** | 代码冲突 |
| **根本原因** | 备份函数 `__bak_generateCtripBookmarklet` 与正式函数 `generateCtripBookmarklet` 同时存在 |
| **解决方案** | 运行修复脚本删除备份函数： |
| **修复脚本** | `fix_controller.php` |
| **手动修复** | 删除 `__bak_generateCtripBookmarklet` 函数，保留 `generateCtripBookmarklet` |
| **预防措施** | 使用版本控制（Git）管理代码，避免手动备份函数 |

---

### 问题 #007：PHP 语法错误 - 严格类型声明

| 项目 | 内容 |
|------|------|
| **问题描述** | PHP 8.0+ 严格类型检查报错 |
| **错误信息** | `TypeError` 或 `Argument must be of type X, Y given` |
| **问题类型** | 类型不匹配 |
| **根本原因** | 代码中使用了 `declare(strict_types=1)`，但传入参数类型不匹配 |
| **解决方案** | 1. 确保传入参数类型正确<br>2. 或移除严格类型声明（不推荐） |
| **预防措施** | 编写单元测试，确保参数类型正确 |

---

## 💾 数据库问题

### 问题 #008：数据库编码问题

| 项目 | 内容 |
|------|------|
| **问题描述** | 中文显示乱码或存储失败 |
| **错误信息** | `Incorrect string value` |
| **问题类型** | 编码配置 |
| **根本原因** | 数据库或表未使用 utf8mb4 编码 |
| **解决方案** | 1. 创建数据库时指定编码：<br>`CREATE DATABASE hotel_admin CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;`<br>2. 修改表编码：<br>`ALTER TABLE table_name CONVERT TO CHARACTER SET utf8mb4;` |
| **预防措施** | 统一使用 utf8mb4 编码，避免使用 utf8（MySQL 的 utf8 是 utf8mb3） |

---

### 问题 #009：SQLite 转 MySQL 数据迁移

| 项目 | 内容 |
|------|------|
| **问题描述** | 需要将 SQLite 数据迁移到 MySQL |
| **问题类型** | 数据迁移 |
| **解决方案** | 使用 `export_mysql.php` 脚本导出 |
| **操作步骤** | 1. 确保 SQLite 数据库存在：`runtime/hotel_admin.db`<br>2. 运行：`php export_mysql.php`<br>3. 导入 MySQL：`mysql -u root -p hotel_admin < database_mysql.sql` |
| **注意事项** | 1. 类型转换可能不完全匹配<br>2. 大数据量导出可能较慢<br>3. 导出前备份数据 |
| **相关文件** | `export_mysql.php` |

---

### 问题 #010：数据库连接失败

| 项目 | 内容 |
|------|------|
| **问题描述** | 无法连接到数据库 |
| **错误信息** | `SQLSTATE[HY000] [1049] Unknown database` 或 `Access denied` |
| **问题类型** | 连接配置 |
| **根本原因** | 1. 数据库不存在<br>2. 用户名/密码错误<br>3. 主机/端口错误 |
| **解决方案** | 检查 `.env` 文件配置： |
| **配置示例** | ```env<br>DB_TYPE = mysql<br>DB_HOST = 127.0.0.1<br>DB_NAME = hotel_admin<br>DB_USER = root<br>DB_PASS = your_password<br>DB_PORT = 3306<br>DB_CHARSET = utf8mb4<br>``` |
| **预防措施** | 使用环境变量管理不同环境的配置 |

---

### 问题 #021：MySQL 连接失败 - 多重配置路径不一致 【本次复盘】

| 项目 | 内容 |
|------|------|
| **问题描述** | 登录页面无法登录，提示数据库访问被拒绝 |
| **错误信息** | `Access denied for user 'root'@'localhost' (using password: YES)` |
| **问题类型** | 数据库 + 配置 |
| **发生时间** | 2026-04-04 23:15 |
| **根本原因** | **三层配置路径不一致**：<br>1. MySQL 实际安装路径：`C:\xampp`<br>2. `my.ini` 配置指向：`D:\yingyong\xampp`（错误）<br>3. `.env` 数据库密码与 MySQL 实际密码不一致<br>4. `php.ini` 不存在导致 `pdo_mysql` 扩展未加载 |
| **解决步骤** | **步骤1：修复 my.ini 路径配置**<br>`basedir=C:/xampp/mysql`<br>`datadir=C:/xampp/mysql/data`<br>`socket=C:/xampp/mysql/mysql.sock`<br><br>**步骤2：创建 php.ini 并启用扩展**<br>```ini<br>extension=pdo_mysql<br>extension=mysqli<br>```<br><br>**步骤3：同步 MySQL 密码与 .env**<br>将 MySQL root 密码设置为与 `.env` 中 `DB_PASS` 一致（`z123123`）<br><br>**步骤4：清除缓存并重启服务**<br>```cmd<br>rd /s /q runtime\cache<br>taskkill /F /IM php.exe<br>``` |
| **验证命令** | ```cmd<br>:: 检查 MySQL 连接<br>mysql -u root -pz123123 -e "SELECT 1;"<br><br>:: 检查 pdo_mysql 扩展<br>php -m \| findstr pdo_mysql<br><br>:: 测试登录 API<br>curl -X POST http://localhost:8080/api/auth/login -H "Content-Type: application/json" -d "{\"username\":\"admin\",\"password\":\"admin123\"}"<br>``` |
| **相关文件** | `C:\xampp\mysql\bin\my.ini`<br>`C:\xampp\php\windowsXamppPhp\php.ini`<br>`.env`<br>`config/database.php` |
| **预防措施** | 1. **统一路径**：所有配置文件中的路径必须指向实际安装位置<br>2. **密码同步**：MySQL 密码修改后立即更新 `.env`<br>3. **扩展检查**：部署前验证 `php -m` 确认所需扩展已加载<br>4. **环境一致性**：开发环境迁移时检查所有配置文件 |
| **经验总结** | 此次问题涉及 **3 个独立问题叠加**：my.ini 路径错误 + php.ini 缺失 + 密码不一致。排查时应逐层验证（服务启动 → 扩展加载 → 数据库连接 → 密码验证）。 |

---

## 🌐 网络/部署问题

### 问题 #011：跨域请求失败 (CORS)

| 项目 | 内容 |
|------|------|
| **问题描述** | 前端请求 API 时浏览器报错 |
| **错误信息** | `Access-Control-Allow-Origin` 错误 |
| **问题类型** | 跨域限制 |
| **根本原因** | 浏览器同源策略限制 |
| **解决方案** | 1. PHP 后端添加 CORS 头：<br>`header('Access-Control-Allow-Origin: *');`<br>2. 或在路由中间件中配置 |
| **预防措施** | 统一使用 JSONP 或配置 CORS 中间件 |

---

### 问题 #012：API 接口 404

| 项目 | 内容 |
|------|------|
| **问题描述** | 访问 API 接口返回 404 |
| **错误信息** | `404 Not Found` |
| **问题类型** | 路由配置 |
| **根本原因** | 1. URL 错误<br>2. 路由未定义<br>3. 伪静态未配置 |
| **解决方案** | 1. 检查路由定义：`route/app.php`<br>2. Nginx/Apache 配置伪静态<br>3. 使用 `index.php` 入口 |
| **Nginx配置** | ```nginx<br>location / {<br>    try_files $uri $uri/ /index.php?$query_string;<br>}<br>``` |
| **预防措施** | 测试环境使用 PHP 内置服务器避免配置问题 |

---

## 📊 数据问题

### 问题 #013：销售间夜数据显示为0

| 项目 | 内容 |
|------|------|
| **问题描述** | 美团销售间夜榜数据始终显示为0 |
| **问题类型** | 数据解析错误 |
| **根本原因** | 1. 数据路径错误：`data.data.data.peerRankData` → `data.peerRankData`<br>2. 缺少 `aiMetricName` 字段提取<br>3. `aiMetricName` 判断值错误 |
| **解决方案** | 修正代码中的数据提取逻辑： |
| **正确判断** | ```javascript<br>// 销售间夜: P_XS_PAY_ROOM_NIGHT<br>// 销售额: P_XS_PAY_AMT<br>// 入住间夜: P_RZ_NIGHT_COUNT<br>// 房费收入: P_RZ_ROOM_PAY<br>const isSalesRoomNights = aiMetricName.includes('P_XS') && aiMetricName.includes('ROOM_NIGHT');<br>const isSales = aiMetricName.includes('P_XS') && aiMetricName.includes('AMT');<br>const isRoomNights = aiMetricName.includes('P_RZ_NIGHT_COUNT');<br>const isRoomRevenue = aiMetricName.includes('P_RZ_ROOM_PAY');<br>``` |
| **相关文件** | `public/index.html` 数据提取部分 |
| **预防措施** | 添加调试日志，确认数据结构后再写解析逻辑 |

---

### 问题 #014：Excel 导入失败

| 项目 | 内容 |
|------|------|
| **问题描述** | 上传 Excel 文件后解析失败 |
| **错误信息** | 文件格式错误或解析异常 |
| **问题类型** | 文件解析 |
| **根本原因** | 1. 文件格式不支持（必须是 .xlsx）<br>2. Python 脚本权限问题<br>3. 表头不匹配 |
| **解决方案** | 1. 确保安装 Python 3.x 和 openpyxl<br>2. 检查文件格式为 .xlsx<br>3. 使用正确的表头模板 |
| **预防措施** | 提供下载模板功能，限制上传格式 |

---

### 问题 #015：月累计数据计算错误

| 项目 | 内容 |
|------|------|
| **问题描述** | 日报表月累计数据不准确 |
| **问题类型** | 业务逻辑 |
| **根本原因** | 跨月数据计算逻辑错误 |
| **解决方案** | 检查 `DailyReport` 模型中的月累计计算方法 |
| **预防措施** | 添加单元测试验证计算逻辑 |

---

## 🔐 权限/认证问题

### 问题 #016：Token 认证失败

| 项目 | 内容 |
|------|------|
| **问题描述** | API 返回 401 未授权 |
| **错误信息** | `Unauthorized` 或 `Token invalid` |
| **问题类型** | 认证失效 |
| **根本原因** | 1. Token 过期<br>2. Token 未正确传递<br>3. Token 格式错误 |
| **解决方案** | 1. 重新登录获取新 Token<br>2. 检查请求头：`Authorization: {token}`<br>3. 清除浏览器缓存 |
| **预防措施** | 实现 Token 自动刷新机制 |

---

### 问题 #017：酒店禁用后权限未失效

| 项目 | 内容 |
|------|------|
| **问题描述** | 酒店禁用后用户仍能查看数据 |
| **问题类型** | 权限控制 |
| **根本原因** | 缓存未更新或权限检查遗漏 |
| **解决方案** | 1. 清除用户权限缓存<br>2. 检查 `Base.php` 中的酒店状态验证 |
| **预防措施** | 酒店状态变更时触发权限缓存刷新 |

---

## ⚡ 性能问题

### 问题 #018：页面加载缓慢

| 项目 | 内容 |
|------|------|
| **问题描述** | 首页或数据列表加载时间过长 |
| **问题类型** | 性能优化 |
| **根本原因** | 1. 数据量过大未分页<br>2. 未使用索引<br>3. N+1 查询问题 |
| **解决方案** | 1. 添加分页<br>2. 为常用查询字段添加索引<br>3. 使用 JOIN 替代循环查询 |
| **预防措施** | 使用 EXPLAIN 分析 SQL，添加查询缓存 |

---

### 问题 #019：内存溢出

| 项目 | 内容 |
|------|------|
| **问题描述** | PHP 报错 `Allowed memory size exhausted` |
| **错误信息** | `Fatal error: Allowed memory size of X bytes exhausted` |
| **问题类型** | 内存限制 |
| **根本原因** | 处理大量数据时内存不足 |
| **解决方案** | 1. 增加内存限制：`ini_set('memory_limit', '512M');`<br>2. 分批处理数据<br>3. 使用生成器 |
| **预防措施** | 大数据量处理使用队列或分批处理 |

---


### 问题 #20：数据库连接超时 【自动归档】

| 项目 | 内容 |
|------|------|
| **问题描述** | 数据库连接超时 |
| **问题类型** | 数据库 |
| **发生次数** | 4 次（达到归档阈值） |
| **首次出现** | 2026-04-02 |
| **最后出现** | 2026-04-02 |
| **错误信息** | Connection timed out after 30s |
| **根本原因** | ⚠️ 待补充 - 请分析并填写 |
| **解决方案** | ⚠️ 待补充 - 请填写详细解决步骤 |
| **预防措施** | ⚠️ 待补充 - 请填写如何避免再次发生 |
| **自动归档时间** | 2026-04-02 |

> 📝 **注意**: 本问题已达到 4 次发生阈值，系统自动归档。
> 请开发者补充完整的问题分析和解决方案。

---

## 🧭 当前项目重点问题归档（2026-05）

> 人工归档规则：同类问题出现 3 次以上，或虽未达到 3 次但影响登录、数据库、OTA 抓取、核心页面、数据安全、Git 恢复能力的重点问题，必须记录到本库。

### 问题 #022：OTA Cookie 失效导致携程/美团抓取失败 【高频】

| 项目 | 内容 |
|------|------|
| **问题描述** | 携程/美团线上数据抓取接口返回失败，或平台提示登录失效 |
| **错误信息** | API 返回 `401` / `403`，或 OTA 平台返回“登录失效” |
| **问题类型** | OTA 数据抓取 / 认证 |
| **归档依据** | `DEV_LOG.md` 标记为高频问题 |
| **根本原因** | 携程/美团 Cookie 有效期通常只有 1-7 天；Cookie 过期、平台登录态变化或书签脚本回传了旧 Cookie |
| **解决方案** | 1. 管理员重新登录对应 OTA 后台<br>2. 重新运行书签脚本回传 Cookie<br>3. 再触发 `/api/online-data/fetch-ctrip` 或 `/api/online-data/fetch-meituan` 验证抓取<br>4. 不要把 Cookie、spidertoken 写入聊天、文档或 Git |
| **相关文件** | `app/controller/OnlineData.php`、`route/app.php` |
| **预防措施** | 增加 Cookie 过期预警；定时抓取前先检查 Cookie 更新时间和最近一次成功抓取时间 |
| **记录时间** | 2026-05-18 |

---

### 问题 #023：美团 API 日期格式错误导致返回空数据 【高频】

| 项目 | 内容 |
|------|------|
| **问题描述** | 美团接口请求成功，但页面或入库数据为空 |
| **错误信息** | 无明确后端异常，表现为接口返回空列表或业务指标为 0 |
| **问题类型** | OTA 数据 / 接口参数 |
| **归档依据** | `DEV_LOG.md` 标记为高频问题 |
| **根本原因** | 美团要求日期格式 `YYYYMMDD`，携程使用 `YYYY-MM-DD`；如果复用携程日期格式，美团接口会返回空数据 |
| **解决方案** | 在美团抓取逻辑中统一转换日期：`str_replace('-', '', $startDate)`，所有美团流量、竞对、评论接口都必须走平台专属日期格式 |
| **相关文件** | `app/controller/OnlineData.php`、`scripts/auto_fetch_online_data.php` |
| **预防措施** | 为 OTA 平台建立参数格式清单；新增接口时先确认日期、酒店 ID、分页、指标字段格式 |
| **记录时间** | 2026-05-18 |

---

### 问题 #024：Vite 构建误覆盖 `public/index.html` 【重大】

| 项目 | 内容 |
|------|------|
| **问题描述** | 执行前端构建后，主项目首页异常或空白 |
| **错误信息** | `public/index.html` 从约 1.28 MB / 17,000 行变为约 552 字节 |
| **问题类型** | 前端构建 / 工程事故 |
| **归档依据** | `DEV_LOG.md` 重大踩坑；`AGENTS.md` 明确禁止在 `public/` 下执行 Vite build |
| **根本原因** | 在 ThinkPHP 主项目 `public/` 目录下执行了 Vite 构建，Vite 将 `public/index.html` 当作入口并覆盖原单文件 Vue 页面 |
| **解决方案** | 1. 立即停止继续构建<br>2. 用 Git 恢复 `public/index.html` 到正确版本<br>3. 确认文件体积和行数恢复正常<br>4. 后续只在 `hotel-frontend/` 中执行 Vite 构建 |
| **相关文件** | `public/index.html`、`hotel-frontend/`、`package.json` |
| **预防措施** | 修改 `public/index.html` 前先看 `git status`；构建命令限定到 `hotel-frontend/`；重要前端修改后及时提交 Git |
| **记录时间** | 2026-05-18 |

---

### 问题 #025：`.env` 与数据库配置不一致导致连接失败 【重点】

| 项目 | 内容 |
|------|------|
| **问题描述** | 后端无法连接 MySQL，登录或接口请求失败 |
| **错误信息** | `Access denied for user 'root'@'localhost'`，或数据库连接失败 |
| **问题类型** | 数据库 / 环境配置 |
| **归档依据** | `DEV_LOG.md`、`PROJECT_HANDOFF.md` 均记录该风险 |
| **根本原因** | `.env` 会覆盖 `config/database.php` 默认配置；`.env` 缺失、`DB_PASS` 与本机 MySQL 不一致、数据库名不是 `hotelx` 都会导致连接失败 |
| **解决方案** | 1. 确认 `.env` 存在<br>2. 以本机 MySQL 实际配置为准同步 `DB_HOST`、`DB_NAME`、`DB_USER`、`DB_PASS`、`DB_PORT`<br>3. 本项目默认数据库名为 `hotelx`<br>4. 修改后重启 PHP 服务并访问 `/api/health` 验证 |
| **相关文件** | `.env`、`.example.env`、`config/database.php`、`QUICK_START.md` |
| **预防措施** | `.env` 不提交 Git；`.example.env` 保持与当前推荐配置一致；不要在文档中记录真实生产密码 |
| **记录时间** | 2026-05-18 |

---

### 问题 #026：OPcache 或运行时缓存导致代码修改不生效 【重点】

| 项目 | 内容 |
|------|------|
| **问题描述** | 已修改 PHP 控制器或中间件，但接口返回仍是旧逻辑 |
| **错误信息** | 无固定错误信息，表现为“代码已改但结果不变” |
| **问题类型** | 运行环境 / 缓存 |
| **归档依据** | `DEV_LOG.md` 已记录为踩坑问题 |
| **根本原因** | PHP OPcache 或 ThinkPHP `runtime/cache`、`runtime/temp` 缓存了旧代码或旧配置 |
| **解决方案** | 1. 开发环境关闭 OPcache<br>2. 清理 `runtime/cache`、`runtime/temp`<br>3. 必要时重启 PHP 内置服务器或 Apache<br>4. 若接口提供清缓存能力，可调用 `/api/online-data/clear-cache` |
| **相关文件** | `runtime/cache`、`runtime/temp`、`config/cache.php`、`app/controller/OnlineData.php` |
| **预防措施** | 开发环境关闭 OPcache；修改控制器、中间件、配置后统一清缓存再验证 |
| **记录时间** | 2026-05-18 |

---

### 问题 #027：只导入 `hotelx_dump.sql` 导致核心业务表或字段缺失 【重点】

| 项目 | 内容 |
|------|------|
| **问题描述** | 新环境导入数据库后，部分模块接口失败或页面数据异常 |
| **错误信息** | 可能出现 `Base table or view not found`、`Unknown column`，或启动检查提示核心表缺失 |
| **问题类型** | 数据库 / 初始化 |
| **归档依据** | `MISSING_TABLES.md` 记录完整 SQL 检查结论 |
| **根本原因** | `hotelx_dump.sql` 是基础 dump，只包含 12 张基础表；当前完整项目依赖 `database/init_full.sql` 及迁移补齐的 52 张表和新增字段 |
| **解决方案** | 1. 新环境不要单独导入 `hotelx_dump.sql`<br>2. 创建 `hotelx` 数据库后导入 `database/init_full.sql`<br>3. 运行 `C:\xampp\php\php.exe scripts\verify_sql_schema_contract.php` 验证表和字段契约<br>4. 需要单文件完整 SQL 时运行 `scripts/build_hotelx_full_dump.ps1` |
| **相关文件** | `database/init_full.sql`、`hotelx_dump.sql`、`MISSING_TABLES.md`、`scripts/verify_sql_schema_contract.php` |
| **预防措施** | 数据库结构变更后同步迁移和完整初始化入口；交付新环境时以 `database/init_full.sql` 为准 |
| **记录时间** | 2026-05-18 |

---

### 问题 #028：本机 PHP CLI 不在 PATH 导致命令执行失败 【重点】

| 项目 | 内容 |
|------|------|
| **问题描述** | 运行 `php think`、验证脚本或定时抓取脚本失败 |
| **错误信息** | `php` 不是内部或外部命令，或脚本无法找到 PHP 可执行文件 |
| **问题类型** | 运行环境 |
| **归档依据** | `CODEX_HANDOFF.md`、`QUICK_START.md` 均记录本机需优先使用 XAMPP PHP 路径 |
| **根本原因** | 当前机器未把 PHP 加入 PATH，项目依赖 XAMPP 中的 `php.exe` |
| **解决方案** | 优先使用完整路径：`C:\xampp\php\php.exe think run --host 127.0.0.1 --port 8080`；如果 XAMPP 在 D 盘，改为对应安装路径；也可使用 `start-hotel.bat` 自动检测 |
| **相关文件** | `start-hotel.bat`、`QUICK_START.md`、`CODEX_HANDOFF.md` |
| **预防措施** | 所有脚本优先检测常见 XAMPP PHP 路径；文档命令避免假设 `php` 已在 PATH |
| **记录时间** | 2026-05-18 |

---

### 问题 #029：PowerShell 中文路径编码导致脚本路径乱码 【重点】

| 项目 | 内容 |
|------|------|
| **问题描述** | 在中文路径下执行脚本时，路径被显示或解析为乱码，导致文件找不到或命令失败 |
| **错误信息** | 路径出现 `妗岄潰`、`鍥介檯` 等乱码 |
| **问题类型** | 运行环境 / 脚本编码 |
| **归档依据** | `DEV_LOG.md` 已记录该踩坑问题；当前项目路径包含中文目录 |
| **根本原因** | PowerShell 脚本编码、终端代码页和中文路径不一致 |
| **解决方案** | 1. 使用 `-LiteralPath` 处理中文路径<br>2. 脚本文件保存为 UTF-8<br>3. 对仍有乱码的历史脚本，改用 `cmd /c` 或完整绝对路径执行 |
| **相关文件** | `start-hotel.bat`、各类 PowerShell 验证脚本 |
| **预防措施** | 新增脚本必须兼容中文路径；不要拼接未转义路径；优先使用绝对路径和 `-LiteralPath` |
| **记录时间** | 2026-05-18 |

---

### 问题 #030：携程点评抓取返回 0 条或失败 【重点】

| 项目 | 内容 |
|------|------|
| **问题描述** | 携程点评页面有数据，但宿析OS抓取接口返回 0 条或失败 |
| **错误信息** | 接口成功但数据为空，或携程接口返回鉴权/参数异常 |
| **问题类型** | OTA 数据抓取 / 携程点评 |
| **归档依据** | `CODEX_HANDOFF.md` 记录为近期继续调试重点 |
| **根本原因** | `Request URL`、`Cookie`、`spidertoken`、`Payload JSON` 不是同一次刷新后的 `getCommentList` 请求；或 Payload 不完整 |
| **解决方案** | 1. 在携程商家后台点评页刷新页面<br>2. Network 中找到同一次请求的 `getCommentList`<br>3. 只填写 `Request URL`、Headers 中的 `Cookie`、Headers 中的 `spidertoken`、完整 `Payload JSON`<br>4. 不填写响应头、HTTP/2 伪头、状态码等无关字段 |
| **相关文件** | `app/controller/OnlineData.php`、`route/app.php` |
| **预防措施** | 前端表单继续遮蔽敏感字段；调试时只保留必要请求字段；不要把 Cookie 或 spidertoken 写入文档和 Git |
| **记录时间** | 2026-05-18 |

---

## 🔧 快速修复命令集

### PHP 服务管理
```cmd
# 启动 PHP 服务器（端口 8081）
cd /d c:\Users\Admin\Desktop\JDXM\JDSJ\HOTEL\public
C:\xampp\php\php.exe -S 0.0.0.0:8081 router.php

# 清理 PHP 进程
taskkill /F /IM php.exe

# 检查端口占用
netstat -ano | findstr :8081
```

### 外网访问安全

本项目不再使用 ngrok 或其他临时公网隧道。外部访问必须走正式受控环境，至少包含 HTTPS、身份认证、IP 白名单和访问日志。


### 数据库管理
```cmd
# 导出 SQLite 到 MySQL
php export_mysql.php

# MySQL 导入
mysql -u root -p hotel_admin < database_mysql.sql

# 检查 MySQL 状态
tasklist | findstr mysqld
```

### 代码修复
```cmd
# 修复 OnlineData.php 重复函数
php fix_controller.php

# 清理缓存
rd /s /q runtime\cache
```

---

## 📋 问题记录模板

发现新问题请按以下格式记录：

```markdown
### 问题 #0XX：问题标题

| 项目 | 内容 |
|------|------|
| **问题描述** | 简明描述问题 |
| **错误信息** | 完整的错误信息 |
| **问题类型** | 运行环境/代码/数据库/网络/数据/权限/性能 |
| **根本原因** | 分析问题产生的原因 |
| **解决方案** | 详细的解决步骤 |
| **相关文件** | 涉及修改的文件 |
| **预防措施** | 如何避免再次发生 |
| **记录时间** | YYYY-MM-DD |
```

---

**最后更新时间**: 2026-05-18
**版本**: v1.1
**维护人**: 开发团队

---

> 💡 **提示**: 遇到 3 次以上无法解决的问题，或影响登录、数据库、OTA 数据、核心页面、数据安全、Git 恢复能力的重点问题，请按模板格式添加到本库中，方便团队共享知识。
