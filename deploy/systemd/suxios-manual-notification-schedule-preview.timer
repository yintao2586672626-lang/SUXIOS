[Unit]
Description=Check SUXIOS manual notification schedule every minute in preview mode

[Timer]
OnCalendar=*-*-* *:*:00
AccuracySec=10s
Persistent=false
Unit=suxios-manual-notification-schedule-preview.service

# Deliberately not enableable until an operator reviews command registration,
# migration state, and target identity.
